import{c as e}from"./api-CS_DHR0v.js";var t={A2aServerConfig:{bind:`Optional advertise-only host override for card endpoint URLs. The
routes always serve on the gateway's own listener; this only changes
the host printed in advertised endpoints when A2A is fronted at a
different address. \`None\` derives from the gateway host.`,enabled:"Master switch for the inbound A2A surface. Default `false`: no\nwell-known route, no per-alias cards, no inbound endpoints.",port:"Optional advertise-only port override, paired with `bind`. `None`\nderives from the gateway port. Advertise-only: nothing binds here.",public_base_url:`Operator-supplied base URL advertised in agent card endpoints.`},A2aServerSection:{server:"Inbound A2A discovery server (`[a2a.server]`)."},AcpConfig:{default_agent:"Agent alias to use when `session/new` omits `agentAlias` and more than\none agent is configured. When exactly one agent exists it is\nauto-selected regardless of this field.",max_sessions:"Maximum number of concurrent ACP sessions. Default: `10`.",session_timeout_secs:"Idle session timeout in seconds. Sessions with no activity for this\nduration are eligible for eviction. Default: `3600` (1 hour)."},AgentA2aConfig:{exposed_skills:`Filter selecting which resolved skill ids appear on this alias's
card. Empty = no skills advertised. Entries that do not resolve to
a real skill in the alias's bundles are dropped (the bundles are
canonical; this only selects from them).`,published:`Publish this alias as a discoverable A2A agent. Default \`false\`:
the alias is excluded from the discovery catalog and serves no
per-alias card even when the server is enabled.`},AgentMemoryConfig:{backend:"The backend kind this agent uses. Defaults to `Sqlite` for new\nagents; once an agent has on-disk data the value is locked."},AgentWorkspaceConfig:{access:`Cross-agent workspace allowlist. An empty map grants no sibling access.`,path:"Optional explicit workspace path. `None` = derive from\n`<install>/agents/<alias>/workspace/`.",read_memory_from:`Cross-agent memory allowlist. An empty list grants access only to local memory.`,unrestricted_filesystem:`Escape hatch: when \`true\`, the agent can read or write anywhere
the host filesystem permits. Off by default; flipping this on is
auditable.`},Ai21ModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},AihubmixModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},AliasedAgentConfig:{a2a:"Per-agent A2A publication block (`[agents.<alias>.a2a]`). Gates\nwhether this alias is discoverable as a spec-conforming A2A agent\nand which resolved skills appear on its card. Default-closed\n(`published = false`, no exposed skills). See\n`crate::multi_agent::AgentA2aConfig`.",acp_enable_mcp:"Initialize this agent's `mcp_bundles` tools when it serves an ACP\n(`session/new`) session.\n\nOff by default: MCP servers are external processes/services that can\nblock startup while they connect, and ACP `session/new` is expected to\nreturn promptly. Enable it when this agent must call its `mcp_bundles`\ntools over ACP; `session/new` then pays the one-time MCP connection cost\n(bounded and non-fatal per server). Set per agent so each ACP profile\nopts in independently; when this agent is the ACP default\n(`acp.default_agent`, or the sole configured agent), the flag is picked\nup automatically for sessions that omit `agentAlias`.",channels:'Channel aliases this agent handles (e.g. `["telegram.<alias>", "discord.<alias>"]`).\nEach entry is a `ChannelRef` resolving through `[channels.<type>.<alias>]`;\n`Config::validate()` fails loud on dangling references.',classifier_provider:`Optional override for the per-message LLM reply-intent classifier
(\`classify_channel_reply_intent\` in zeroclaw-channels). When non-empty,
the channel orchestrator routes the "should this message be replied to?"
classification call to \`[providers.models.<type>.<alias>]\` referenced
here, instead of reusing the main agent's \`model_provider\`.

Source of truth for api_key / uri / model / temperature etc. is the
referenced \`[providers.models.<type>.<alias>]\` entry. This field is
a reference only (NEVER a copy), per AGENTS.md SINGLE SOURCE OF TRUTH.

Empty (\`Default\`) = inherit the main agent's resolved provider+model
(preserves pre-PR behavior; backward compatible).

Use case: classification is a cheap REPLY/NO_REPLY decision, doesn't
need a high-end model. Point this at a fast/free small model
(e.g. \`kimi-k2.5\`, \`qwen-turbo\`) while \`model_provider\` stays on the
expensive answering model (e.g. \`qwen3.6-plus\`).

Note: TOML table names cannot contain \`.\`, so alias \`kimi-k2.5\`
must be written as \`[providers.models.custom.kimi-k2-5]\`. The
underlying \`model = "kimi-k2.5"\` string can still contain dots.

ACP channels (IDE-direct) always reply and skip the classifier
entirely, so this field has no effect on ACP traffic.`,cron_jobs:`Cron job aliases. Each entry references \`cron[key]\`, a declarative
scheduled job invoked by the scheduler on its configured trigger.
When the cron fires, this agent is the actor that executes the job.`,delegate_same_risk_profile:"Auto-allow delegation to every agent sharing this agent's risk\nprofile. Default `true` preserves the historical reach where any\nsame-profile peer is a delegation target. Set `false` to opt this\nagent out so only the explicit `delegates` list is reachable.\nGating (whether delegation is permitted at all) still lives on the\nrisk profile's `delegation_policy.mode`; this only narrows reach.",delegates:`Explicit delegate roster: additional agent aliases this agent may
delegate to, beyond same-profile peers. Possibly empty. String
entries are accepted for concise manual editing and load as bounded
delegates; saved config emits object entries with explicit modes.
Entries may name agents on a different risk profile. \`Config::validate()\`
fails loud on a dangling alias, duplicate alias, or self-reference.`,enabled:`Whether this agent is active. Set false to disable without removing the definition.`,identity:`Per-agent identity format (\`[agents.<alias>.identity]\`). Each
agent renders its own IDENTITY.md / SOUL.md inside its
per-agent workspace; this block selects the format (OpenClaw or
AIEOS) and optional inline/file source for the agent's identity
document.`,knowledge_bundles:`Knowledge bundle aliases. Additive: the agent loads every listed
bundle.`,mcp_bundles:"MCP bundle aliases. Each entry references `mcp_bundles[key]`, a named\ngroup of MCP servers. Secure by default: an agent is granted only the\nservers named by its bundles. An agent with no `mcp_bundles` receives\nno MCP servers (omission is not a grant). See\n`Config::mcp_servers_for_agent`.",memory:"Per-agent memory backend selection (`[agents.<alias>.memory]`).\nThe `backend` field is locked at agent creation and immutable on\nsubsequent loads. Defaults to `Sqlite`. See\n`crate::multi_agent::AgentMemoryConfig`.",model_provider:'Dotted model-provider alias (e.g. `"anthropic.<alias>"`).\nResolves through `model_providers.<type>.<alias>` at runtime;\n`Config::validate()` fails loud on dangling references.',precheck:`Per-agent reply-intent precheck controls. The classifier call reads
this block at message time; model/provider selection stays on
\`classifier_provider\` so there is only one routing source of truth.`,risk_profile:'Risk profile alias (e.g. `"default"`). Resolves delegation guardrails at runtime.',runtime_profile:'Runtime profile alias (e.g. `"default"`). Resolves agentic/iteration settings.',skill_bundles:`Skill bundle aliases. Each entry resolves to
\`skill_bundles[key].directory\` at runtime; the agent loads every
listed bundle.`,summary_provider:"Per-agent override for the context-compression summarizer provider, as\na `providers.models.<type>.<alias>` reference. Empty (Default) = inherit\nthe runtime profile's `context_compression.summary_provider`, else the\nagent's own resolved provider+model. Reference only, never a copy;\nresolved by [`Config::effective_summary_provider`]. Validated in\n`Config::validate()`.",transcription_provider:'Transcription / STT provider as a dotted alias reference\n(`<type>.<alias>`, e.g. `"groq.<alias>"`). Resolves through\n`transcription_providers.<type>.<alias>`. Empty = agent has no\ntranscription preference; channels that ingest voice still need a\nresolved provider (there is no global default), so an inbound voice\nflow into an agent with empty `transcription_provider` errors loudly\nat the channel boundary.',tts_provider:'TTS provider as a dotted alias reference (`<type>.<alias>`,\ne.g. `"openai.<alias>"`). Resolves through `tts_providers.<type>.<alias>`.\nEmpty = no TTS for this agent (there is no global default-provider concept;\nevery agent that wants TTS sets its own `tts_provider`).',workspace:`Per-agent workspace block (\`[agents.<alias>.workspace]\`).
Holds the agent's filesystem path, cross-agent access allowlist,
filesystem-escape boolean, and cross-agent memory allowlist.
Default is fully jailed (no cross-agent access). See
\`crate::multi_agent::AgentWorkspaceConfig\`.`},AmqpConfig:{amqp_url:"AMQP broker URL. Use `amqp://` for plain or `amqps://` for TLS\n(e.g. `amqps://fedora:@rabbitmq.fedoraproject.org/%2Fpublic_pubsub`).",ca_cert:"Path to the CA certificate bundle for `amqps://` connections.",client_cert:`Path to the client certificate for broker mutual-TLS auth
(Fedora Messaging requires a client cert).`,client_key:"Path to the client private key matching `client_cert`.",content_template:`Template for the inbound message content. \`{field}\` placeholders are
interpolated from the JSON delivery body's top-level keys. When empty,
the raw delivery body is used verbatim.`,dispatch:"Where consumed deliveries are routed: drive an agent turn\n(`agent_loop`, default), dispatch to the SOP engine (`sop`), or both\n(`sop_and_agent_loop`). The `sop` and `sop_and_agent_loop` modes match\nthe delivery against SOP `amqp` triggers by routing key.",durable_ack:`Acknowledgement mode. When \`true\` (default), deliveries are acked only
after the message is durably handed to the agent loop, giving
at-least-once semantics: a crash before hand-off redelivers the event.
Set \`false\` for at-most-once (broker acks on dispatch), which silently
drops in-flight events on crash and is only appropriate for
non-side-effecting, drop-on-overload consumers.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",exchange:"Exchange to bind the consumer queue to (e.g. `amq.topic`).",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,queue:`Queue name. Leave unset for a server-generated, transient,
auto-deleted, exclusive queue. Set a stable name (UUID recommended)
only when durable delivery across reconnects is required.`,routing_keys:"Routing keys to bind. Scope these to the topics of interest; binding\n`#` consumes the entire exchange and is almost never what you want.",sender_label:"Value placed in `ChannelMessage.sender` for every delivery from this\nsource (e.g. `anitya`). Lets the orchestrator's self-loop guard and\nper-channel routing identify the origin.",thread_id_field:"Dotted path into the JSON delivery body whose value becomes the\nmessage `thread_ts`, correlating replies to the originating event\n(e.g. `message.project.name`). Empty disables threading."},AnthropicModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},AnyscaleModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},ApprovalGroupConfig:{members:`Identity labels that belong to this group.`},ApprovalPolicyConfig:{escalation_route:'Route to escalate to on timeout (the distinct "second route"). `None`/empty\nre-surfaces to the same route (today\'s `Escalate` behavior). When configured,\nthe route must use the `channel:recipient` format.',quorum:"Distinct approvers required before the gate clears. `0`/`1` both mean a\nsingle approval; `>= 2` requires a quorum of distinct approver identities.",request_route:"Channel to deliver the INITIAL approval request to when a run parks at a\ngate this policy governs, formatted `channel:recipient` (e.g.\n`discord.ops:123456789012345678`). The `channel` names a configured channel\n(the `<channel>.<alias>` / bare-`<channel>` key from the channel map); the\n`recipient` is that channel's addressee (a Discord channel id, a chat id,\netc.). Delivery is best-effort - the gate is the source of truth and a\ndelivery failure never blocks or clears it; approvals still come back\nthrough the normal HTTP/WS/tool surfaces. `None`/empty = no out-of-band\nrequest notice (today's behavior: only the originating surface is notified).\nThis is a DISTINCT lifecycle event from `escalation_route`: the request goes\nout when the run parks; the escalation goes out only if it later times out.\nWhen configured, the route must use the `channel:recipient` format.",required_group:"Group whose members may satisfy this policy's gate. `None`/empty = any\nprincipal permitted by `approval_mode` (back-compat, no membership gate)."},ApprovalRoute:{approver_channel:`A registered channel name (NOT the originator) — the distinct-approver hop.`,on_no_approver:`Behavior when the approver is unreachable. Fail-closed by default.`,timeout_secs:`Bound the approver's response window; a timeout denies (DoS guard). Default 120s.`},ArceeModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},AssemblyAiSttConfig:{api_key:`AssemblyAI API key.`},AssemblyAiTranscriptionProviderConfig:{api_key:`API key for the transcription provider.`,initial_prompt:`Whisper-style initial prompt to bias the model toward expected
vocabulary (proper nouns, technical terms). Provider-specific support;
silently ignored where not applicable.`,language:'Optional language hint passed to the provider (ISO-639-1 like `"en"` /\n`"ru"`, or BCP-47 like `"en-US"` for Google). Most providers auto-detect\nwhen this is unset.'},AstraiModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},AtomicChatModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},AuditConfig:{enabled:`Enable audit logging`,log_path:`Path to audit log file (relative to zeroclaw dir)`,max_size_mb:`Maximum log size in MB before rotation`,sign_events:`Sign events with HMAC for tamper evidence`},AvianModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},AzureModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,api_version:"Azure API version string (e.g. `2024-10-21`).",chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,deployment:`Azure deployment name: the deployment created in Azure AI Studio.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",resource:"Azure resource name (the `<resource>` part of `<resource>.openai.azure.com`).",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},BackupConfig:{compress:`Compress backup archives.`,destination_dir:`Output directory for backup archives (relative to workspace root).`,enabled:"Enable the `backup` tool.",encrypt:`Encrypt backup archives (requires a configured secret store key).`,include_dirs:`Workspace subdirectories to include in backups.`,max_keep:`Maximum number of backups to keep (oldest are pruned).`,schedule_cron:`Optional cron expression for scheduled automatic backups.`,schedule_timezone:"IANA timezone for `schedule_cron`."},BaichuanModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},BasetenModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},BedrockModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,region:"AWS region for the Bedrock endpoint (e.g. `us-east-1`, `eu-west-1`).",replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},BlueskyConfig:{app_password:`App-specific password (from Bluesky settings).`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,handle:'Bluesky handle (e.g. `"mybot.bsky.social"`).'},BoundTriggerSource:{condition:"The payload contract a `condition` on this source may reference. Absent\nfor sources whose triggers carry no condition (`cron`, `manual`,\n`webhook`)."},BrowserComputerUseConfig:{allow_remote_endpoint:`Allow remote/public endpoint for computer-use sidecar (default: false)`,api_key:`Optional bearer token for computer-use sidecar`,endpoint:`Sidecar endpoint for computer-use actions (OS-level mouse/keyboard/screenshot)`,max_coordinate_x:`Optional X-axis boundary for coordinate-based actions`,max_coordinate_y:`Optional Y-axis boundary for coordinate-based actions`,timeout_ms:`Per-action request timeout in milliseconds`,window_allowlist:`Optional window title/process allowlist forwarded to sidecar policy`},BrowserConfig:{allowed_domains:"Allowed domains for `browser_open` (exact or subdomain match)",allowed_private_hosts:'Private/internal hosts allowed to bypass SSRF protection.\nExact and subdomain matches are supported; `["*"]` permits **all** private/local\nhosts (RFC 1918, loopback, link-local, `.local`). Default: empty (deny).\nListed hosts also bypass `allowed_domains`. Both the `browser` tool and\n`browser_open` accept `http://` for listed hosts — internal services\nfrequently lack a public TLS cert.\nWarning: `["*"]` also reaches link-local addresses, including the cloud metadata\nendpoint (`169.254.169.254`) — list specific hosts unless you accept that exposure.',backend:`Browser automation backend: "agent_browser" | "rust_native" | "computer_use" | "auto"`,computer_use:`Computer-use sidecar configuration`,enabled:"Enable `browser_open` tool (opens URLs in the system browser without scraping)",headed:`Show browser window for agent_browser backend. When unset, inherits AGENT_BROWSER_HEADED.`,native_chrome_path:`Optional Chrome/Chromium executable path for rust-native backend`,native_headless:`Headless mode for rust-native backend`,native_webdriver_url:"WebDriver endpoint URL for rust-native backend (e.g. `http://127.0.0.1:9515`)",session_name:`Browser session name (for agent-browser automation)`},BuiltinHooksConfig:{command_logger:`Enable the command-logger hook (logs tool calls for auditing).`,webhook_audit:"Configuration for the webhook-audit hook.\n\nWhen enabled, POSTs a JSON payload to `url` for every tool invocation\nthat matches one of `tool_patterns`."},CerebrasModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},ChannelPrecheckConfig:{enabled:"When false, the precheck is skipped entirely for this agent and every\naccepted channel message triggers the full agent loop. Default: `true`.",timeout_secs:"Hard ceiling (seconds) on the precheck LLM call. On timeout the\nprecheck fails open to REPLY. Default: `5`."},ChannelTriggerKind:{condition:`Condition payload contract for a channel-message trigger. Channel
payloads are arbitrary, so this is the \`open\` contract; carried per row
so the builder reads one shape whether the source is bound or a channel.`},ChannelsConfig:{ack_reactions:"Whether to add acknowledgement reactions (👀 on receipt, ✅/⚠️ on\ncompletion) to incoming channel messages. Default: `true`.",amqp:"AMQP channel instances (`[channels.amqp.<alias>]`).",bluesky:"Bluesky channel instances (`[channels.bluesky.<alias>]`).",clawdtalk:"ClawdTalk voice channel instances (`[channels.clawdtalk.<alias>]`).",cli:"Enable the CLI interactive channel. Default: `true`.",debounce_ms:"Inbound message debounce window in milliseconds. When a sender fires\nmultiple messages within this window, they are accumulated and dispatched\nas a single concatenated message. `0` disables debouncing. Default: `0`.",dingtalk:"DingTalk channel instances (`[channels.dingtalk.<alias>]`).",discord:"Discord bot channel instances (`[channels.discord.<alias>]`).",email:"Email channel instances (`[channels.email.<alias>]`).",filesystem:"Filesystem SOP listener instances (`[channels.filesystem.<alias>]`).",git:"Git-forge channel instances (`[channels.git.<alias>]`). GitHub is\nthe first provider; the `provider` field selects the forge.",gmail_push:"Gmail Pub/Sub push notification channel instances (`[channels.gmail_push.<alias>]`).",imessage:"iMessage channel instances (`[channels.imessage.<alias>]`, macOS only).",irc:"IRC channel instances (`[channels.irc.<alias>]`).",lark:"Lark channel instances (`[channels.lark.<alias>]`).",line:"LINE Messaging API channel instances (`[channels.line.<alias>]`).",linq:"Linq Partner API channel instances (`[channels.linq.<alias>]`).",matrix:"Matrix channel instances (`[channels.matrix.<alias>]`).",mattermost:"Mattermost bot channel instances (`[channels.mattermost.<alias>]`).",max_concurrent_per_channel:`Per-channel multiplier for the global channel message in-flight budget.
Runtime multiplies this value by the configured channel count, then
applies its global minimum and maximum bounds to one shared dispatcher
semaphore. Default: \`4\`.`,message_timeout_secs:`Base timeout in seconds for processing a single channel message (LLM + tools).
Runtime uses this as a per-turn budget that scales with tool-loop depth
(up to 4x, capped) so one slow/retried model call does not consume the
entire conversation budget.
Default: 300s for on-device LLMs (Ollama) which are slower than cloud APIs.`,mochat:"Mochat customer service channel instances (`[channels.mochat.<alias>]`).",mqtt:"MQTT channel instances (`[channels.mqtt.<alias>]`).",nextcloud_talk:"Nextcloud Talk bot channel instances (`[channels.nextcloud_talk.<alias>]`).",qq:"QQ Official Bot channel instances (`[channels.qq.<alias>]`).",reddit:"Reddit channel instances (`[channels.reddit.<alias>]`).",session_backend:'Session persistence backend: `"jsonl"` (legacy) or `"sqlite"` (new default).\nSQLite provides FTS5 search, metadata tracking, and TTL cleanup.',session_persistence:"Persist channel conversation history to JSONL files so sessions survive\ndaemon restarts. Files are stored in `{workspace}/sessions/`. Default: `true`.",session_ttl_hours:"Auto-archive stale sessions older than this many hours. `0` disables. Default: `0`.",show_tool_calls:"Whether to send tool-call notification messages (e.g. `🔧 web_search_tool: …`)\nto channel users. When `false`, tool calls are still logged server-side but\nnot forwarded as individual channel messages. Default: `false`.",signal:"Signal channel instances (`[channels.signal.<alias>]`).",slack:"Slack bot channel instances (`[channels.slack.<alias>]`).",telegram:"Telegram bot channel instances (`[channels.telegram.<alias>]`).",twitch:"Twitch chat channel instances (`[channels.twitch.<alias>]`).",twitter:"X/Twitter channel instances (`[channels.twitter.<alias>]`).",voice_call:"Voice call channel instances (`[channels.voice_call.<alias>]`).",voice_duplex:"Voice duplex instances (`[channels.voice_duplex.<alias>]`).",voice_wake:"Voice wake word detection channel instances (`[channels.voice_wake.<alias>]`).",wati:"WATI WhatsApp Business API channel instances (`[channels.wati.<alias>]`).",webhook:"Webhook channel instances (`[channels.webhook.<alias>]`).",wechat:"WeChat personal iLink Bot channel instances (`[channels.wechat.<alias>]`).",wecom:"WeCom (WeChat Enterprise) Bot Webhook channel instances (`[channels.wecom.<alias>]`).",wecom_ws:"WeCom AI Bot WebSocket channel instances (`[channels.wecom_ws.<alias>]`).",whatsapp:"WhatsApp channel instances (`[channels.whatsapp.<alias>]`)."},ClassificationRule:{hint:"Must match a `[[model_routes]]` hint value.",keywords:`Case-insensitive substring matches.`,max_length:`Only match if message length <= N chars.`,min_length:`Only match if message length >= N chars.`,patterns:'Case-sensitive literal matches (for "```", "fn ", etc.).',priority:`Higher priority rules are checked first.`},ClaudeCodeConfig:{allowed_tools:`Claude Code tools the subprocess is allowed to use`,enabled:"Enable the `claude_code` tool",env_passthrough:`Extra env vars passed to the claude subprocess (e.g. ANTHROPIC_API_KEY for API-key billing)`,max_output_bytes:`Maximum output size in bytes (2MB default)`,system_prompt:`Optional system prompt appended to Claude Code invocations`,timeout_secs:`Maximum execution time in seconds (coding tasks can be long)`},ClaudeCodeRunnerConfig:{enabled:"Enable the `claude_code_runner` tool",session_ttl:`Session time-to-live in seconds before auto-cleanup (default: 3600)`,ssh_host:`SSH host for session handoff links (e.g. "myhost.example.com")`,tmux_prefix:`Prefix for tmux session names (default: "zc-claude-")`},ClawdTalkConfig:{enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`},CloudOpsConfig:{cost_threshold_monthly_usd:`Monthly USD threshold to flag cost items. Default: 100.0.`,default_cloud:`Default cloud model_provider for analysis context. Default: "aws".`,enabled:`Enable cloud operations tools. Default: false.`,iac_tools:"Supported IaC tools for review. Default: \\[`terraform`\\].",supported_clouds:"Supported cloud model_providers. Default: [`aws`, `azure`, `gcp`].",well_architected_frameworks:"Well-Architected Frameworks to check against. Default: \\[`aws-waf`\\]."},CloudflareModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},CloudflareTunnelConfig:{token:`Cloudflare Tunnel token (from Zero Trust dashboard)`},CodexCliConfig:{enabled:"Enable the `codex_cli` tool",env_passthrough:`Extra env vars passed to the codex subprocess (e.g. OPENAI_API_KEY)`,extra_args:`Extra CLI arguments appended to \`codex exec\` before the prompt.

Values come from operator-controlled config (same trust level as
\`env_passthrough\`) and are not validated — the operator is responsible
for understanding the implications of flags passed here.

**Warning:** \`--sandbox=danger-full-access\` disables Codex's bubblewrap
isolation; only use in environments where the container itself provides
isolation (e.g. Kubernetes pods with restricted PSS).

Example: \`["--sandbox=danger-full-access", "--skip-git-repo-check"]\``,max_output_bytes:`Maximum output size in bytes (2MB default)`,timeout_secs:`Maximum execution time in seconds (coding tasks can be long)`},CohereModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},ComposioConfig:{api_key:`Composio API key (stored encrypted when secrets.encrypt = true)`,enabled:`Enable Composio integration for 1000+ OAuth tools`,entity_id:`Default entity ID for multi-user setups`},Config:{a2a:"Inbound A2A discovery server (`[a2a.server]`). Default-closed:\nserves the well-known catalog card and per-alias agent cards only\nwhen `enabled = true`. See `crate::multi_agent::A2aServerConfig`.",acp:"ACP (Agent Client Protocol) server configuration (`[acp]`).",agents:`Aliased agents in this install. Each entry under \`[agents.<alias>]\`
is one user-facing agent with its own identity, channels, model
provider, risk profile, workspace, and memory scope.
\`DelegateTool\` consults this map when one agent delegates a
subtask to another.`,backup:"Backup tool configuration (`[backup]`).",browser:"Browser automation configuration (`[browser]`).",browser_delegate:'Browser delegation configuration (`[browser_delegate]`).\n\nDelegates browser-based tasks to a browser-capable CLI subprocess (e.g.\nClaude Code with `claude-in-chrome` MCP tools). Useful for interacting\nwith corporate web apps (Teams, Outlook, Jira, Confluence) that lack\ndirect API access. A persistent Chrome profile can be configured so SSO\nsessions survive across invocations.\n\nFields:\n- `enabled` (`bool`, default `false`) — enable the browser delegation tool.\n- `cli_binary` (`String`, default `"claude"`) — CLI binary to spawn for browser tasks.\n- `chrome_profile_dir` (`String`, default `""`) — Chrome user-data directory for\n  persistent SSO sessions. When empty, a fresh profile is used each invocation.\n- `allowed_domains` (`Vec<String>`, default `[]`) — allowlist of domains the browser\n  may navigate to. Empty means all non-blocked domains are permitted.\n- `blocked_domains` (`Vec<String>`, default `[]`) — denylist of domains. Blocked\n  domains take precedence over allowed domains.\n- `task_timeout_secs` (`u64`, default `120`) — per-task timeout in seconds.\n\nCompatibility: additive and disabled by default; existing configs remain valid when omitted.\nRollback/migration: remove `[browser_delegate]` or keep `enabled = false` to disable.',channels:"Channel configurations: Telegram, Discord, Slack, etc. (`[channels]`).",claude_code:"Claude Code tool configuration (`[claude_code]`).",claude_code_runner:"Claude Code task runner with Slack progress and SSH session handoff (`[claude_code_runner]`).",cloud_ops:"Cloud transformation accelerator configuration (`[cloud_ops]`).",codex_cli:"Codex CLI tool configuration (`[codex_cli]`).",composio:"Composio managed OAuth tools integration (`[composio]`).",conversational_ai:`Conversational AI agent builder configuration (\`[conversational_ai]\`).

Experimental / future feature — not yet wired into the agent runtime.
Omitted from generated config files when disabled (the default).
Existing configs that already contain this section will continue to
deserialize correctly thanks to \`#[serde(default)]\`.`,cost:"Cost tracking and budget enforcement configuration (`[cost]`).\nAlso hosts the operator-managed rate sheet at\n`[cost.rates.<type>.<model>]`.",cron:`Declarative cron jobs (\`[cron.<alias>]\`), alias-keyed.

Each entry is a named scheduled job synced into the database at
scheduler startup. Subsystem runtime knobs (enable/disable, catch-up,
run-history retention) live on \`[scheduler]\`.`,data_retention:"Data retention and purge configuration (`[data_retention]`).",delegate:"Delegate tool global default configuration (`[delegate]`).",embedding_routes:"Embedding-routing rules — route `hint:<name>` to specific\nmodel_provider + model combos for embedding requests.",escalation:"Escalation routing configuration (`[escalation]`).",eval:"Agent evaluation harness (`[eval]`) — surfaced via `zeroclaw eval`.\nDistinct from `[agent.eval]`, which is the in-loop response-quality scorer.",file_download:"Standalone file download tool configuration (`[file_download]`).",file_upload:"Standalone file upload tool configuration (`[file_upload]`).",file_upload_bundle:"Standalone multi-file bundle upload tool configuration\n(`[file_upload_bundle]`).",gateway:"Gateway server configuration: host, port, pairing, rate limits (`[gateway]`).",gemini_cli:"Gemini CLI tool configuration (`[gemini_cli]`).",google_workspace:"Google Workspace CLI (`gws`) tool configuration (`[google_workspace]`).",hardware:`Hardware configuration (wizard-driven physical world setup).`,heartbeat:"Heartbeat configuration for periodic health pings (`[heartbeat]`).",hooks:`Hooks configuration (lifecycle hooks and built-in hook toggles).`,http_request:"HTTP request tool configuration (`[http_request]`).",image_gen:"Standalone image generation tool configuration (`[image_gen]`).",jira:"Jira integration configuration (`[jira]`).",knowledge:"Knowledge graph configuration (`[knowledge]`).",knowledge_bundles:"Named knowledge bundles (`[knowledge_bundles.<alias>]`).",link_enricher:"Link enricher configuration (`[link_enricher]`).",linkedin:"LinkedIn integration configuration (`[linkedin]`).",locale:`Locale for tool descriptions (e.g. \`"en"\`, \`"zh-CN"\`).

When set, tool descriptions shown in system prompts are loaded from
Fluent \`.ftl\` locale files. Falls back to embedded English, then to
hardcoded descriptions.

If omitted or empty, the locale is auto-detected from the host
system's locale (defaulting to \`"en"\` if that can't be determined).`,mcp:"External MCP server connections (`[mcp]`).",mcp_bundles:"Named MCP server bundles (`[mcp_bundles.<alias>]`).",media_pipeline:"Automatic media understanding pipeline (`[media_pipeline]`).",memory:"Memory backend configuration: sqlite, markdown, embeddings (`[memory]`).",microsoft365:"Microsoft 365 Graph API integration (`[microsoft365]`).",model_routes:"Model-routing rules — route `hint:<name>` to specific\nmodel_provider + model combos.",multimodal:"Multimodal (image) handling configuration (`[multimodal]`).",node_transport:"Secure inter-node transport configuration (`[node_transport]`).",nodes:"Dynamic node discovery configuration (`[nodes]`).",notion:"Notion integration configuration (`[notion]`).",observability:"Observability backend configuration (`[observability]`).",onboard_state:"Meta-state for the Quickstart flow (which sections the user has\nalready walked through). Not user-facing config (`[onboard_state]`).",opencode_cli:"OpenCode CLI tool configuration (`[opencode_cli]`).",pacing:"Pacing controls for slow/local LLM workloads (`[pacing]`).",peer_groups:"Named peer groups (`[peer_groups.<name>]`). Each entry binds a\nchannel, a list of member agents, and optional non-agent\n(external) members and a per-group blocklist. Mutual opt-in:\ntwo agents become peers only when both appear in the same\ngroup's `agents`. Empty by default for single-agent installs.\nSee `crate::multi_agent::PeerGroupConfig`.",peripherals:"Peripheral board configuration for hardware integration (`[peripherals]`).",pipeline:"Pipeline tool configuration (`[pipeline]`).",plugins:"Plugin system configuration (`[plugins]`).",project_intel:"Project delivery intelligence configuration (`[project_intel]`).",providers:"All configured provider profiles, grouped by category under a\nsingle `[providers]` root. Categories today: `models`, `tts`,\n`transcription`. Shape: `[providers.<category>.<type>.<alias>]`,\ne.g. `[providers.models.anthropic.default]`,\n`[providers.tts.openai.default]`,\n`[providers.transcription.groq.default]`.",proxy:"Proxy configuration for outbound HTTP/HTTPS/SOCKS5 traffic (`[proxy]`).",query_classification:`Automatic query classification — maps user messages to model hints.`,reliability:"Reliability settings: retries, backoff, key rotation (`[reliability]`).",risk_profiles:"Named risk/autonomy profiles (`[risk_profiles.<alias>]`).",runtime:"Runtime adapter configuration (`[runtime]`). Controls native vs Docker execution.",runtime_profiles:"Named runtime/LLM execution profiles (`[runtime_profiles.<alias>]`).",scheduler:"Scheduler configuration for periodic task execution (`[scheduler]`).",schema_version:`Config file schema version.`,secrets:"Secrets encryption configuration (`[secrets]`).",security:"Security subsystem configuration (`[security]`).",security_ops:"Managed cybersecurity service configuration (`[security_ops]`).",shell_tool:"Shell tool configuration (`[shell_tool]`).",skill_bundles:"Named skill bundles (`[skill_bundles.<alias>]`).",skills:"Skills loading and community repository behavior (`[skills]`).",sop:"Standard Operating Procedures engine configuration (`[sop]`).",storage:"Persistent storage model_provider configuration (`[storage]`).",text_browser:"Text browser tool configuration (`[text_browser]`).",todotracker:"ZeroCode live task tracker (`[todotracker]`), the read-only\nTodoWrite visual tracker in the Code pane.",transcription:`Voice transcription configuration (Whisper API via Groq).`,trust:"Trust scoring and regression detection configuration (`[trust]`).",tts:"Text-to-Speech configuration (`[tts]`).",tunnel:"Tunnel configuration for exposing the gateway publicly (`[tunnel]`).",verifiable_intent:"Verifiable Intent (VI) credential verification and issuance (`[verifiable_intent]`).",web_fetch:"Web fetch tool configuration (`[web_fetch]`).",web_search:"Web search tool configuration (`[web_search]`).",wss:"WebSocket Secure (WSS) transport for remote TUI connections (`[wss]`)."},ConfigApiError:{code:`Stable error code for programmatic matching.`,message:`Human-readable message. Safe to render directly in dashboards / terminals.`,op_index:"Index into the JSON Patch operation array, when the error originated\nfrom a specific op in a `PATCH /api/config` batch.",path:"Property path the error pertains to, when applicable. Empty when the\nerror is whole-config (e.g. `ReloadFailed`)."},ContextCompressionConfig:{enabled:"The runtime context compressor was removed; no runtime execution path\nconsumes this flag, so setting it to `true` currently has no effect.\nDefaults to `false` to match actual runtime behavior;\n`Config::collect_warnings` reads an explicit `true` only to report\n`context_compression_unsupported`.",summary_model:`DEPRECATED bare model id retained as a compatibility fallback.`,summary_provider:"Summarizer provider as a `<type>.<alias>` reference into `providers.models`."},ConversationalAiConfig:{analytics_enabled:`Enable conversation analytics tracking. Default: false (privacy-by-default).`,auto_detect_language:`Automatically detect user language from message content. Default: true.`,conversation_timeout_secs:`Conversation timeout in seconds (inactivity). Default: 1800.`,default_language:`Default language for conversations (BCP-47 tag). Default: "en".`,enabled:`Enable conversational AI features. Default: false.`,escalation_confidence_threshold:`Intent confidence below this threshold triggers escalation. Default: 0.3.`,knowledge_base_tool:`Optional tool name for RAG-based knowledge base lookup during conversations.`,max_conversation_turns:`Maximum conversation turns before auto-ending. Default: 50.`,supported_languages:"Supported languages for conversations. Default: [`en`, `de`, `fr`, `it`]."},CopilotModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},CostConfig:{allow_override:`Allow requests to exceed budget with --override flag (default: false)`,daily_limit_usd:`Daily spending limit in USD (default: 10.00)`,enabled:`Enable cost tracking (default: true)`,enforcement:`Cost enforcement behavior when budget limits are approached or exceeded.`,monthly_limit_usd:`Monthly spending limit in USD (default: 100.00)`,rates:`Operator-managed rate sheet at \`[cost.rates.*]\`. Sections mirror
the \`[providers.*]\` dotted-path exactly with the trailing \`alias\`
segment replaced by the resource the rate applies to (model id,
tool name, …). Layout:

\`\`\`toml
[cost.rates.providers.models.anthropic."claude-opus-4-7"]
input_per_mtok = 15.0
output_per_mtok = 75.0
cached_input_per_mtok = 1.5

[cost.rates.providers.tts.openai."tts-1-hd"]
per_mchar = 30.0

[cost.rates.providers.transcription.openai.whisper-1]
per_minute = 0.006

[cost.rates.tools.web_search]
per_call = 0.005
\`\`\``,track_per_agent:`Stamp each recorded cost entry with the originating agent alias so
\`/api/cost?agent=<alias>\` and CLI rollups can attribute spend to a
specific agent. Disable on high-volume deployments if the extra
HashMap aggregation shows up in profiles (default: true).`,warn_at_percent:`Warn when spending reaches this percentage of limit (default: 80)`},CostEnforcementConfig:{mode:`Enforcement mode: "warn", "block", or "route_down".`,reserve_percent:`Reserve this percentage of budget for critical operations.`,route_down_model:`Model hint to route to when budget is exceeded (used with "route_down" mode).`},CostRatesConfig:{providers:"`[cost.rates.providers.*]` — rates for everything under\n`[providers.*]` (models, TTS, transcription, …).",tools:"`[cost.rates.tools.<name>]` — per-call rates for tools that\nhit paid APIs. Keyed by the tool's registered name."},CronJobDecl:{allowed_tools:`Optional allowlist of tool names for agent jobs. When omitted, scheduler
defaults may still exclude scheduler mutation tools for cron agent jobs.`,command:'Shell command to run (required when `job_type = "shell"`).',delivery:`Delivery configuration.`,enabled:"Whether the job is enabled. Default: `true`.",job_type:'Job type: `"shell"` (default) or `"agent"`.',model:`Model override for agent jobs.`,name:`Human-readable name.`,prompt:'Agent prompt (required when `job_type = "agent"`).',schedule:`Schedule for the job.`,session_target:'Session target: `"isolated"` (default) or `"main"`.',shell_output_format:'Output format for shell jobs: `"wrapped"` (default) or `"raw"`.\n\n- `"wrapped"` (default): returns stdout and stderr wrapped in a\n  `status=... / stdout: / stderr:` envelope.\n- `"raw"`: returns trimmed stdout when the process exits zero. A\n  non-zero process exit still gets the wrapped status/stdout/stderr\n  envelope for diagnosis. Failures that never reach a process exit —\n  a security-policy denial, a shell setup/spawn failure, or a\n  timeout — return a plain error string in either format.',uses_memory:"Whether to recall and inject memory context before this agent job runs.\nDefaults to `true`; set to `false` for stateless digest jobs."},CustomModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},CustomTunnelConfig:{health_url:`Optional URL to check tunnel health`,start_command:`Command template to start the tunnel. Use {port} and {host} placeholders.
Example: "bore local {port} --to bore.pub"`,url_pattern:`Optional regex to extract public URL from command stdout`},DataRetentionConfig:{categories:`Limit retention enforcement to specific data categories (empty = all).`,dry_run:`Preview what would be deleted without actually removing anything.`,enabled:"Enable the `data_management` tool.",retention_days:`Days of data to retain before purge eligibility.`},DeepgramSttConfig:{api_key:`Deepgram API key.`,model:`Deepgram model name (default: "nova-2").`},DeepgramTranscriptionProviderConfig:{api_key:`API key for the transcription provider.`,initial_prompt:`Whisper-style initial prompt to bias the model toward expected
vocabulary (proper nouns, technical terms). Provider-specific support;
silently ignored where not applicable.`,language:'Optional language hint passed to the provider (ISO-639-1 like `"en"` /\n`"ru"`, or BCP-47 like `"en-US"` for Google). Most providers auto-detect\nwhen this is unset.',model:'Deepgram model name (default: `"nova-2"`).'},DeepinfraModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},DeepmystModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},DeepseekModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},DelegateTargetConfig:{agent:`Target agent alias.`,mode:`Whether this target is parent-bounded or independent.`},DelegateToolConfig:{agentic_timeout_secs:`Default timeout in seconds for agentic sub-agent runs.
Can be overridden per-agent in \`[agents.<name>]\` config.
Default: 300 seconds.`,timeout_secs:`Default timeout in seconds for non-agentic sub-agent model_provider calls.
Can be overridden per-agent in \`[agents.<name>]\` config.
Default: 120 seconds.`},DeliveryConfigDecl:{best_effort:"Best-effort delivery. Default: `true`.",channel:'Channel name (e.g. `"telegram"`, `"discord"`).',mode:'Delivery mode: `"none"` or `"announce"`.',thread_id:`Optional thread/conversation identifier carried into the outbound send.
Required by channels that route on a separate \`thread_id\` field (e.g.
webhook callbacks bridging into agent-chat platforms).`,to:`Target/recipient identifier.`},DingTalkConfig:{client_id:`Client ID (AppKey) from DingTalk developer console`,client_secret:`Client Secret (AppSecret) from DingTalk developer console`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only."},DiscordConfig:{approval_timeout_secs:"Seconds to wait for operator approval on `always_ask` tools before auto-denying.",archive:"When true, the channel opens a sidecar `discord.db` SQLite memory\nbackend, archives every non-bot message it sees, and registers the\n`discord_search` tool against it. Default: false. Folded in from\nthe legacy `[channels.discord-history]` block.",bot_token:"Discord bot token (from Discord Developer Portal). `#[serde(default)]`\nfor the same reason as `TelegramConfig::bot_token`: a missing token\nmust deserialize as an empty string instead of salvage-dropping the\nalias; `validate_bot_token` still requires a real token once\n`enabled = true`.",channel_ids:`Channel IDs to watch. Empty = watch every channel the bot can see.
Used by the archive sidecar (when \`archive = true\`) and by the
in-channel filter when set.`,draft_update_interval_ms:'Minimum interval (ms) between draft message edits to avoid rate limits.\nOnly used when `stream_mode = "partial"`.',enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,guild_ids:`Guild (server) IDs to restrict the bot to. Empty = listen across all
guilds the bot is invited to. Migrated from the legacy \`guild_id\`
singular field.`,intents_mask:`Raw gateway intent mask override. When set, this exact value is sent
in IDENTIFY instead of the derived mask: an operator escape hatch
for downstream deployments that consume gateway events through
custom builds or forks. The operator owns the consequences:
privileged bits still need their Developer Portal toggles, and
dropping baseline bits silences message handling (a warning is
logged). Unset (default) = derive the mask from the channel's
feature config.`,interrupt_on_new_message:`When true, a newer Discord message from the same sender in the same channel
cancels the in-flight request and starts a fresh response with preserved history.`,listen_to_bots:`When true, process messages from other bots (not just humans).
The bot still ignores its own messages to prevent feedback loops.`,mention_only:`When true, only respond to messages that @-mention the bot.
Other messages in the guild are silently ignored.`,multi_message_delay_ms:'Delay (ms) between sending each message chunk in multi-message mode.\nOnly used when `stream_mode = "multi_message"`.',proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",reaction_notifications:"Which inbound reactions to record: `off` (default: reaction events\nare not even requested from the gateway), `own` (reactions to the\nbot's messages), or `all` (reactions to any message passing the\nchannel's filters). Recorded reactions are archived to `discord.db`\nwhen `archive` is enabled, and removed again when a user removes\ntheir reaction (bulk clears, remove-all / remove-emoji, are not\nyet swept). A set `intents_mask` overrides the derived mask: if it\nomits the reaction intents, nothing is recorded.",reply_min_interval_secs:"Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",reply_queue_depth_max:"Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",slash_command_scope:"Scope for registered slash commands: `global` (default, application-wide,\n~1h propagation) or `guild` (registered to each `guild_ids` entry,\ninstant). Only meaningful when `slash_commands = true`; `guild` with an\nempty `guild_ids` warns and falls back to global. Switching scope reaps\nowned commands from the now-inactive scope; note that *removing* a guild\nfrom `guild_ids` (without switching scope) does not reap that guild's\ncommands - remove the bot from the guild, or switch scope, to clear them.",slash_commands:"When true, register and serve Discord slash commands (e.g. `/ask`)\nover the Gateway WebSocket, in addition to message handling. Default\nfalse. (Prototype: currently registers a single `/ask <prompt>`.)",stall_timeout_secs:`Stall-watchdog timeout in seconds. When non-zero, the bot will abort
and retry if no progress is made within this duration. 0 = disabled.`,stream_mode:"Streaming mode for progressive response delivery.\n`off` (default): single message. `partial`: editable draft updates.\n`multi_message`: split response into separate messages at paragraph boundaries."},DockerRuntimeConfig:{allowed_workspace_roots:`Optional workspace root allowlist for Docker mount validation.`,cpu_limit:"Optional CPU limit (`None` = no explicit limit).",image:`Runtime image used to execute shell commands.`,memory_limit_mb:"Optional memory limit in MB (`None` = no explicit limit).",mount_workspace:"Mount configured workspace into `/workspace`.",network:"Docker network mode (`none`, `bridge`, etc.).",read_only_rootfs:`Mount root filesystem as read-only.`},DoubaoModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},DriftEntry:{drifted:"Always `true` when surfaced. Present so secret entries unambiguously\ncommunicate the drift signal in shape `{path, secret: true, drifted: true}`.",in_memory_value:`In-memory value (the daemon's view). Absent for secrets.`,on_disk_value:`On-disk value (what the file contains right now). Absent for secrets.`,secret:"`true` for secret fields where values cannot be exposed."},EdgeTtsProviderConfig:{api_key:`API key (openai, elevenlabs, google).`,binary_path:`Path to backend binary (edge-tts subprocess; piper local server).`,language_code:"Language code for google (e.g. `en-US`).",model:"Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",response_format:'Audio response format sent to the TTS backend (e.g. `"opus"`, `"mp3"`,\n`"wav"`). Defaults to `"opus"` for the OpenAI family. Override to\n`"wav"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `"mp3"` for broader compatibility.',similarity_boost:"Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",speed:"Playback speed multiplier (openai only; default `1.0`).",stability:"Voice stability for elevenlabs (0.0-1.0; default `0.5`).",uri:"Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",voice:"Voice override for this instance. When empty, falls back to\n`[tts].default_voice`."},ElevenLabsTtsProviderConfig:{api_key:`API key (openai, elevenlabs, google).`,binary_path:`Path to backend binary (edge-tts subprocess; piper local server).`,language_code:"Language code for google (e.g. `en-US`).",model:"Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",response_format:'Audio response format sent to the TTS backend (e.g. `"opus"`, `"mp3"`,\n`"wav"`). Defaults to `"opus"` for the OpenAI family. Override to\n`"wav"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `"mp3"` for broader compatibility.',similarity_boost:"Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",speed:"Playback speed multiplier (openai only; default `1.0`).",stability:"Voice stability for elevenlabs (0.0-1.0; default `0.5`).",uri:"Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",voice:"Voice override for this instance. When empty, falls back to\n`[tts].default_voice`."},EmailConfig:{enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,html_body:"When `true` (default), outbound emails are rendered as HTML via Markdown conversion.\nSet to `false` to send plain-text emails instead.",oauth2:`OAuth2 settings for IMAP authentication. When present, IMAP uses
XOAUTH2 instead of plain LOGIN. Required for providers like
Outlook/Hotmail that have deprecated password auth.`,observer_mode:`When true, the daemon observes new mail but never modifies any IMAP flag.`,poll_interval_secs:`Polling interval used when the IMAP server does not advertise the IDLE
capability (RFC 2177). Ignored when IDLE is available.`},EmailOAuth2Config:{client_id:`OAuth2 application client ID (public client; no secret required for device flow).`,device_code_url:"Device-code initiation endpoint\n(e.g. `https://login.microsoftonline.com/consumers/oauth2/v2.0/devicecode`).",scopes:"OAuth2 scopes to request (must include `offline_access` for refresh tokens).",token_url:"Token endpoint for refresh and device-code polling\n(e.g. `https://login.microsoftonline.com/consumers/oauth2/v2.0/token`)."},EmbeddingRouteConfig:{api_key:`Optional API key override for this route's model_provider`,dimensions:`Optional embedding dimension override for this route`,hint:'Route hint name (e.g. "semantic", "archive", "faq")\n`#[serde(default)]` lets the `create_map_key` macro default-construct\nfrom `{}` before the hint gets injected via `set_prop`. Empty strings\nare rejected by `Config::validate()`.',model:"Provider-local embedding model identifier to use with that provider profile\n`#[serde(default)]` is required for `Default` + `create_map_key` construction.\nEmpty strings are rejected by `Config::validate()`.",model_provider:"Dotted embedding-capable provider profile ref\n`#[serde(default)]` is required for `Default` + `create_map_key` construction.\nEmpty strings are rejected by `Config::validate()`."},EscalationConfig:{alert_channels:`Channel names to alert on high/critical escalations (default: empty).

Each name must match a configured channel. Unrecognised names are
logged at WARN level and skipped.`},EstopConfig:{enabled:`Enable emergency stop controls.`,require_otp_to_resume:`Require a valid OTP before resume operations.`,state_file:`File path used to persist estop state.`},EvalHarnessConfig:{mode:"Default execution mode (`replay` or `live`) used when `--mode` is omitted.",suite_dir:"Default directory of `*.json` trace fixtures used when `--suite` is omitted."},ExternalRegistry:{enabled:"Whether this registry is eligible for installs. Default: `true`.",kind:'Registry protocol. Only `"git"` is supported today; other protocols\nare reserved for a future additive release.',name:"Short alias used in `registry:<name>/<skill>` install specs.",url:"Git repository URL of the registry (must expose a top-level `skills/` dir)."},FeatherlessModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},FileDownloadConfig:{headers:"Static HTTP headers attached to every download request — typically an\n`Authorization: Bearer …` token for the upstream endpoint. Same shape as\n`[mcp.servers.*.headers]`.",max_file_size_bytes:`Maximum download size in bytes. Enforced while streaming: the transfer
is aborted and the partial file removed once this ceiling is exceeded,
so an oversized or unbounded body never fully buffers in memory or lands
on disk. Default: 25 MiB.`,timeout_secs:`Request timeout in seconds. Default: 120.`,url:"Download endpoint URL. Tool is disabled when this is `None` or empty.\nThe file to fetch is selected by the `document_id` query parameter."},FileUploadBundleConfig:{field_name:"Multipart form-field name reused across every file part. Default: `file`.",headers:`Static HTTP headers attached to every upload request.`,max_file_size_bytes:`Maximum per-file size in bytes. Default: 10 MiB.`,max_files:`Maximum number of files per call. Default: 16.`,max_response_body_bytes:`Maximum response body bytes to read from the upload endpoint.
Prevents unbounded memory use from a malicious or verbose receiver.
Default: 4096 (4 KiB).`,max_total_size_bytes:`Maximum cumulative size across every file in one call. Default: 32 MiB.`,method:"HTTP method. Only `POST` (default) and `PUT` are accepted.",timeout_secs:`Request timeout in seconds. Default: 120.`,url:"Upload endpoint URL. Tool is disabled when this is `None` or empty."},FileUploadConfig:{field_name:"Multipart form-field name for the file part. Default: `file`.",headers:"Static HTTP headers attached to every upload request. Same shape as\n`[mcp.servers.*.headers]`.",max_file_size_bytes:`Maximum file size in bytes. Larger files are rejected before any
bytes hit the network. Default: 25 MiB.`,method:"HTTP method. Only `POST` (default) and `PUT` are accepted.",timeout_secs:`Request timeout in seconds. Default: 60.`,url:"Upload endpoint URL. Tool is disabled when this is `None` or empty."},FilesystemConfig:{allow_broad_roots:"Watch broad system roots (`/`, `/home`, `/etc`, `/var`, `/proc`,\n`/sys`, `/dev`, `/tmp`) despite the deny-broad-roots default. The\npseudo-filesystems `/proc` and `/sys` would surface kernel object\npaths in SOP payloads and flood the watcher with events. Off unless\nexplicitly enabled.",debounce_ms:`Collapse rapid repeated events per path/kind within this window.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",events:"Event kinds to emit (`created`, `modified`, `deleted`, `renamed`).",exclude:"Glob patterns to exclude. Applied after `include`.",excluded_tools:`Tools excluded from this channel's tool spec.`,follow_symlinks:`Follow symlinks when reading event-path metadata, hash, and content.
Off by default: a symlink under a watched root is rejected before any
metadata/hash/content handling so its target cannot escape the root. When
on, the canonical target must still resolve inside a configured root.`,include:`Glob patterns to include. Empty matches all paths under the roots.`,max_content_bytes:"Maximum file content bytes admitted into a payload when `read_content`,\nand the size ceiling for hashing. `Some(n)` caps reads and hashing at\n`n` bytes; oversize files are skipped rather than truncated. `None`\nremoves the ceiling entirely (unbounded reads). Defaults to\n`Some(65536)` (64 KiB), so the cap applies by default whenever\n`read_content` or hashing runs.",paths:`Root paths to watch. At least one is required.`,read_content:`Read file content into the event payload. Opt-in.`,recursive:`Watch nested paths beneath each root.`,settle_ms:`Wait this long after the last event before reading metadata or content,
giving atomic writes and slow copies time to finish.`},FirecrawlConfig:{api_key_env:`Environment variable name for the Firecrawl API key`,api_url:`Firecrawl API base URL`,enabled:`Enable Firecrawl fallback`,mode:`Firecrawl extraction mode`},FireworksModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},FriendliModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},GatewayClientAuthConfig:{ca_cert_path:`Path to the PEM-encoded CA certificate used to verify client certs.`,enabled:`Enable client certificate verification (default: false).`,pinned_certs:`Optional SHA-256 fingerprints for certificate pinning.
When non-empty, only client certs matching one of these fingerprints are accepted.`,require_client_cert:`Reject connections that do not present a valid client certificate (default: true).`},GatewayConfig:{allow_public_bind:`Allow binding to non-localhost without a tunnel (default: false)`,allow_remote_admin:`Allow authenticated remote callers to use admin endpoints that are
otherwise localhost-only. Currently this gates \`POST /admin/reload\`.
When false (default), those endpoints reject any non-loopback peer.
When true, a non-loopback request is accepted only if it also passes
pairing authentication — which requires \`require_pairing = true\`; with
pairing off a remote caller cannot be authenticated and is rejected, so
this flag never exposes an anonymous remote reload. \`/admin/shutdown\`
and the pairing-code endpoints stay localhost-only regardless.
(default: false)`,allow_self_upgrade:`Allow triggering a self-upgrade (binary swap via \`zeroclaw update\`) from
the dashboard. This is a remote-code-execution-adjacent surface: any
authenticated dashboard user could replace the running binary. Keep off
unless you trust every paired client. (default: false)`,check_updates:`Poll GitHub for newer releases and show an "update available" indicator
on the dashboard version tag. Read-only; does not install anything.
(default: true)`,host:`Gateway host (default: 127.0.0.1)`,idempotency_max_keys:`Maximum distinct idempotency keys retained in memory.`,idempotency_ttl_secs:`TTL for webhook idempotency keys.`,long_running_request_timeout_secs:`HTTP request timeout (seconds) for \`POST /api/cron/{id}/run\`, which
runs jobs synchronously and routinely exceeds the 30s default.
Default: 600s (10 minutes).`,pair_rate_limit_per_minute:"Max `/pair` requests per minute per client key.",paired_tokens:`Paired bearer tokens (managed automatically, not user-edited)`,pairing_dashboard:`Pairing dashboard configuration`,path_prefix:"Optional URL path prefix for reverse-proxy deployments.\nWhen set, all gateway routes are served under this prefix.\nMust start with `/` and must not end with `/`.",port:`Gateway port (default: 42617)`,rate_limit_max_keys:`Maximum distinct client keys tracked by gateway rate limiter maps.`,request_timeout_secs:`HTTP request timeout (seconds) for gateway routes other than the
long-running cron-trigger endpoint. Default: 30s.`,require_pairing:`Require pairing before accepting requests (default: true)`,session_persistence:`Persist gateway WebSocket chat sessions to SQLite. Default: true.`,session_ttl_hours:`Auto-archive stale gateway sessions older than N hours. 0 = disabled. Default: 0.`,tls:"TLS configuration for the gateway server (`[gateway.tls]`).",trust_forwarded_headers:"Trust proxy-forwarded client IP headers (`X-Forwarded-For`, `X-Real-IP`).\nDisabled by default; enable only behind a trusted reverse proxy.",web_dist_dir:`Path to the web dashboard \`dist\` directory. When set, the gateway
serves the compiled frontend from the filesystem instead of requiring
it to be embedded in the binary. Accepts absolute paths or paths
relative to the working directory. When omitted the gateway runs in
API-only mode (no web dashboard) unless auto-detection finds it.`,webhook_rate_limit_per_minute:"Max `/webhook` requests per minute per client key."},GatewayTlsConfig:{cert_path:`Path to the PEM-encoded server certificate file.`,client_auth:`Client certificate authentication (mutual TLS) settings.`,enabled:`Enable TLS for the gateway (default: false).`,key_path:`Path to the PEM-encoded server private key file.`},GeminiCliConfig:{enabled:"Enable the `gemini_cli` tool",env_passthrough:`Extra env vars passed to the gemini subprocess (e.g. GOOGLE_API_KEY)`,max_output_bytes:`Maximum output size in bytes (2MB default)`,timeout_secs:`Maximum execution time in seconds (coding tasks can be long)`},GeminiCliModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,binary_path:"Path to the `gemini` CLI binary. Falls back to `gemini` (PATH lookup).",chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},GeminiModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,auth_mode:"Auth flow. Defaults to `api_key`; `oauth` uses GeminiModelProvider's\nOAuth-cache integration instead of the `api_key` field.",chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",oauth_client_id:"Google OAuth app `client_id`, used when this alias drives ZeroClaw's\nown browser/device-code login flow (`zeroclaw auth login\n--model-provider gemini --profile <alias>`). Operators relying on\nthe upstream `gemini login` tool don't need this; that tool writes\nits own client_id / client_secret into `~/.gemini/oauth_creds.json`.",oauth_client_secret:"Google OAuth app `client_secret`. Set alongside `oauth_client_id`.",oauth_project:"Pin a specific GCP project ID for the OAuth `loadCodeAssist`\ndiscovery call. When unset, the discovery probes for an\nalready-onboarded project on the credential's account. Replaces\n`GOOGLE_CLOUD_PROJECT` / `GOOGLE_CLOUD_PROJECT_ID` env vars.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},GitConfig:{access_token:`Personal access token for Gitea/Forgejo API requests. The token needs
repository read access plus issue/PR comment write access for replies
and reactions. Gitea/Forgejo provider only.`,api_base_url:'Gitea/Forgejo API base URL, including `/api/v1`, for example\n`https://git.example.org/api/v1` (for the public Gitea service:\n`https://gitea.com/api/v1`). Required when `provider` is `"gitea"`\nor `"forgejo"` - there is no default host, because every API\nrequest carries `access_token`; the channel fails closed at\nstartup rather than send the token to an endpoint the operator\nnever named. Gitea/Forgejo provider only.',app_id:`GitHub App ID (shown on the app's settings page). GitHub provider only.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",events:'Per-event routing table, keyed by normalized event type\n(`"issue_comment.created"`, `"pull_request.opened"`,\n`"workflow_run.failed"`, …). Event types absent from the table fall\nback to the conversational default: `issue_comment.created`,\n`issues.opened`, and `pull_request.opened` are delivered as\nmessages (mention-gated); everything else is ignored. Which API\nendpoints are polled is derived from this table; routing an event\ntype is also subscribing to it.',events_backbone:`Also poll the repository Events API (\`/repos/{owner}/{repo}/events\`)
as a broad backbone transport: one conditional (ETag) request per
repository per tick, so an idle repo costs almost nothing. Caveats:
events arrive with a delay of up to ~5 minutes and the feed carries
no Actions/check events (workflow runs always use their dedicated
endpoint). Items also surfaced by a targeted endpoint are
de-duplicated. Default: \`false\`.`,excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,installation_id:`Installation ID to act as. When unset, the app's installations are
listed on first use and a sole installation is auto-selected;
startup fails if the app has zero or multiple installations.`,listen_to_bots:"Process comments authored by other bot accounts. The app's own\ncomments are always ignored. Default: `false`.",mention_only:"Only respond to comments that @-mention the app's bot login.\nDefault: `true`.",poll_interval_secs:"Poll interval in seconds for new issues and comments. Values below\n15 are clamped to 15. Default: `30`.",private_key:`RS256 private key PEM, the contents of the \`.pem\` file GitHub
generates on the app's settings page, inline and encrypted at rest.
Include the BEGIN/END lines. GitHub provider only.`,private_key_path:"Filesystem path to the RS256 private key `.pem` file, read at startup\nwhen `private_key` (inline PEM) is unset. Backward-compatible fallback\nfor configs that predate the inline-PEM field. GitHub provider only.",provider:'Git forge provider. Supported: `"github"`, `"gitea"`, and\n`"forgejo"` (Forgejo uses the Gitea-compatible REST provider).\nDefault: `"github"`.',proxy_url:`Per-channel proxy override for GitHub API requests.`,repos:"Repositories to poll, as `owner/repo`. Empty = every repository\nvisible to the installation."},GitEventRoute:{message:`Deliver the event into the agent loop as a normal channel message.`,sop:`Route the event to the named SOP through channel-sourced SOP ingress.`},GithubModelsModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},GlmModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},GmailPushConfig:{enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`},GoogleSttConfig:{api_key:`Google Cloud API key.`,language_code:`BCP-47 language code (default: "en-US").`},GoogleTranscriptionProviderConfig:{api_key:`API key for the transcription provider.`,initial_prompt:`Whisper-style initial prompt to bias the model toward expected
vocabulary (proper nouns, technical terms). Provider-specific support;
silently ignored where not applicable.`,language:'Optional language hint passed to the provider (ISO-639-1 like `"en"` /\n`"ru"`, or BCP-47 like `"en-US"` for Google). Most providers auto-detect\nwhen this is unset.'},GoogleTtsProviderConfig:{api_key:`API key (openai, elevenlabs, google).`,binary_path:`Path to backend binary (edge-tts subprocess; piper local server).`,language_code:"Language code for google (e.g. `en-US`).",model:"Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",response_format:'Audio response format sent to the TTS backend (e.g. `"opus"`, `"mp3"`,\n`"wav"`). Defaults to `"opus"` for the OpenAI family. Override to\n`"wav"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `"mp3"` for broader compatibility.',similarity_boost:"Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",speed:"Playback speed multiplier (openai only; default `1.0`).",stability:"Voice stability for elevenlabs (0.0-1.0; default `0.5`).",uri:"Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",voice:"Voice override for this instance. When empty, falls back to\n`[tts].default_voice`."},GoogleWorkspaceAllowedOperation:{methods:`Allowed methods for the service/resource/sub_resource combination.`,resource:"Top-level resource name for the service (for example `users` for Gmail or `files` for Drive).",service:"Google Workspace service ID (for example `gmail` or `drive`).",sub_resource:"Optional sub-resource for 4-segment gws commands\n(for example `messages` or `drafts` under `gmail users`).\nWhen present, the entry only matches calls that include this exact sub_resource.\nWhen absent, the entry only matches calls with no sub_resource."},GoogleWorkspaceConfig:{allowed_operations:"Restrict which resource/method combinations the agent can access.\n\nWhen empty (the default), all methods under `allowed_services` remain\navailable for backward compatibility. When non-empty, the runtime denies\nany `(service, resource, sub_resource, method)` combination that is not\nexplicitly listed. `sub_resource` is optional per entry: an entry without\nit matches only 3-segment `gws` calls; an entry with it matches only calls\nthat supply that exact sub_resource value.\n\nEach entry's `service` must appear in `allowed_services` when that list is\nnon-empty; config validation rejects entries that would never match at\nruntime.",allowed_services:`Restrict which Google Workspace services the agent can access.

When empty (the default), the full default service set is allowed (see
struct-level docs). When non-empty, only the listed service IDs are
permitted. Each entry must be non-empty, lowercase alphanumeric with
optional underscores/hyphens, and unique.`,audit_log:"Enable audit logging of every `gws` invocation (service, resource,\nmethod, timestamp). Default: `false`.",credentials_path:"Path to service account JSON or OAuth client credentials file.\n\nWhen `None`, the tool relies on the default `gws` credential discovery\n(`gws auth login`). Set this to point at a service-account key or an\nOAuth client-secrets JSON for headless / CI environments.",default_account:"Default Google account email to pass to `gws --account`.\n\nWhen `None`, the currently active `gws` account is used.",enabled:"Enable the `google_workspace` tool. Default: `false`.",rate_limit_per_minute:"Maximum number of `gws` API calls allowed per minute. Default: `60`.",timeout_secs:"Command execution timeout in seconds. Default: `30`."},GroqModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},GroqTranscriptionProviderConfig:{api_key:`API key for the transcription provider.`,initial_prompt:`Whisper-style initial prompt to bias the model toward expected
vocabulary (proper nouns, technical terms). Provider-specific support;
silently ignored where not applicable.`,language:'Optional language hint passed to the provider (ISO-639-1 like `"en"` /\n`"ru"`, or BCP-47 like `"en-US"` for Google). Most providers auto-detect\nwhen this is unset.',model:'Whisper model name (default: `"whisper-large-v3-turbo"`).'},HardwareConfig:{baud_rate:`Baud rate negotiated on the serial link. 115200 matches the common Arduino / ESP32 bootloader default; bump to 230400+ when your firmware explicitly supports faster rates and you need the throughput.`,enabled:`Opt in to direct physical-hardware control — GPIO pins, USB-tethered microcontrollers (Arduino, ESP32, Nucleo), or SWD/JTAG debug probes. Leave off for software-only use; turning it on without the right transport configured does nothing.`,probe_target:"Target chip identifier for `transport = probe` (e.g. `STM32F401RE`, `nRF52840_xxAA`). Passed straight to probe-rs for flash/debug operations; must match a chip probe-rs recognizes.",serial_port:"TTY path for the `serial` transport — e.g. `/dev/ttyACM0` on Linux, `/dev/tty.usbmodem1` on macOS, `COM3` on Windows. Ignored for other transports.",transport:"How ZeroClaw reaches the hardware: `native` = Linux SBC with direct GPIO access (Raspberry Pi, Orange Pi); `serial` = USB-tethered microcontroller speaking over a TTY; `probe` = SWD/JTAG debug probe driving a target chip via probe-rs; `none` = disabled.",workspace_datasheets:"Index pre-converted `.md` and `.txt` datasheets from the workspace into\na local RAG store, so the agent can look up pin assignments and\nelectrical specs inline when you ask hardware questions. PDFs are not\nparsed as text; download or convert them to `.md`/`.txt` first. Off by\ndefault — turn on once the workspace has relevant text datasheets."},HeartbeatConfig:{adaptive:"Enable adaptive intervals that back off on failures and speed up for\nhigh-priority tasks. Default: `false`.",agent:"Configured agent alias the heartbeat worker runs as. Required\nwhen `enabled = true`; refers to a `[agents.<alias>]` entry.",deadman_channel:"Channel for dead-man's switch alerts (e.g. `telegram`). Falls back to\nthe heartbeat delivery channel.",deadman_timeout_minutes:"Dead-man's switch timeout in minutes. If the heartbeat has not ticked\nwithin this window, an alert is sent. `0` disables. Default: `0`.",deadman_to:"Recipient for dead-man's switch alerts. Falls back to `to`.",enabled:"Enable periodic heartbeat pings. Default: `false`. When enabled,\n`agent` must name a configured agent — there is no default agent\nfor heartbeat to fall through to.",interval_minutes:"Interval in minutes between heartbeat pings. Minimum: `1`. Default: `30`.",load_session_context:"Load the channel session history before each heartbeat task execution so\nthe LLM has conversational context. Default: `false`.\n\nWhen `true`, the session file for the configured `target`/`to` is passed\nto the agent as `session_state_file`, giving it access to the recent\nconversation history — just as if the user had sent a message.",max_interval_minutes:"Maximum interval in minutes when adaptive mode backs off. Default: `120`.",max_run_history:"Maximum number of heartbeat run history records to retain. Default: `100`.",message:"Optional fallback task text when `HEARTBEAT.md` has no task entries.",min_interval_minutes:"Minimum interval in minutes when adaptive mode is enabled. Default: `5`.",target:"Optional delivery channel for heartbeat output (for example: `telegram`).\nWhen omitted, auto-selects the first configured channel.",task_timeout_secs:"Maximum wall-clock seconds allowed for a single agent invocation\n(Phase 1 decision or Phase 2 task execution). `0` disables.\nDefault: `600` (10 minutes).",to:"Optional delivery recipient/chat identifier (required when `target` is\nexplicitly set).",two_phase:`Enable two-phase heartbeat: Phase 1 asks LLM whether to run, Phase 2
executes only when the LLM decides there is work to do. Saves API cost
during quiet periods. Default: \`true\`.`},HooksConfig:{enabled:`Enable lifecycle hook execution.

Hooks run in-process with the same privileges as the main runtime.
Keep enabled hook handlers narrowly scoped and auditable.`},HttpRequestConfig:{allow_private_hosts:`Allow requests to private/LAN hosts (RFC 1918, loopback, link-local, .local).
Default: false (deny private hosts for SSRF protection).`,allowed_domains:`Allowed domains for HTTP requests (exact or subdomain match)`,allowed_private_hosts:"Private/internal hosts explicitly allowed to bypass SSRF protection.\nExact and subdomain matches are supported; `*` permits all private/local hosts.",enabled:"Enable `http_request` tool for API interactions",max_response_size:`Maximum response size in bytes (default: 1MB, 0 = unlimited)`,secrets:"Named authorization secrets for `auth_secret` requests.",timeout_secs:`Request timeout in seconds (default: 30)`},HuggingfaceModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},HunyuanModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},HyperbolicModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},IMessageConfig:{enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,reply_min_interval_secs:"Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",reply_queue_depth_max:"Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged."},IdentityConfig:{aieos_inline:`Inline AIEOS JSON (alternative to file path)`,aieos_path:`Path to AIEOS JSON file (relative to workspace)`,format:`Identity format: "openclaw" (default) or "aieos"`},ImageGenConfig:{api_key_env:`Environment variable name holding the fal.ai API key.`,default_model:`Default fal.ai model identifier.`,enabled:`Enable the standalone image generation tool. Default: false.`},ImageProviderDalleConfig:{api_key_env:`Environment variable name holding the OpenAI API key.`,model:`DALL-E model identifier.`,size:`Image dimensions.`},ImageProviderFluxConfig:{api_key_env:`Environment variable name holding the fal.ai API key.`,model:`Flux model identifier.`},ImageProviderImagenConfig:{api_key_env:`Environment variable name holding the API key.`,project_id_env:`Environment variable for the Google Cloud project ID.`,region:`Vertex AI region.`},ImageProviderStabilityConfig:{api_key_env:`Environment variable name holding the API key.`,model:`Stability model identifier.`},InceptionModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},InitQuery:{section:"Optional section prefix to scope the init pass (e.g. `model_providers`).\nWithout it, every uninitialized nested section gets its defaults."},IrcConfig:{channels:`Channels to join on connect`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,mention_only:`When true, only respond to messages that mention the bot.
Other messages in the channel are silently ignored.`,nickname:`Bot nickname`,nickserv_password:`NickServ IDENTIFY password`,port:`IRC server port (default: 6697 for TLS)`,sasl_password:`SASL PLAIN password (IRCv3)`,server:`IRC server hostname`,server_password:`Server password (for bouncers like ZNC)`,username:`Username (defaults to nickname if not set)`,verify_tls:`Verify TLS certificate (default: true)`},JiraConfig:{allowed_actions:'Actions the agent is permitted to call.\nValid values: `"get_ticket"`, `"search_tickets"`, `"comment_ticket"`,\n`"list_projects"`, `"myself"`, `"list_transitions"`,\n`"transition_ticket"`, `"create_ticket"`.\nDefaults to `["get_ticket"]` (read-only).',api_token:"Jira API token. Encrypted at rest. Falls back to `JIRA_API_TOKEN` env var.",base_url:"Atlassian instance base URL, e.g. `https://yourco.atlassian.net`.",email:`Jira account email used for Basic auth (Cloud).
Omit for Server/DC deployments using Bearer token auth.
An empty string (\`email = ""\`) deserializes as \`None\`. Configs
that round-tripped the empty default to disk would otherwise
silently regress to Basic auth with empty username, since the
email-required validation was dropped when Server/DC Bearer-token
support landed.`,enabled:"Enable the `jira` tool. Default: `false`.",timeout_secs:"Request timeout in seconds. Default: `30`."},KiloCliModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,binary_path:"Path to the `kilo` CLI binary. Falls back to `kilo` (PATH lookup).",chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},KiloModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,endpoint:`Kilo endpoint variant. Defaults to the canonical Kilo AI Gateway.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},KnowledgeBundleConfig:{sources:`Paths or URLs to include in this knowledge bundle.`,tags:`Tags for filtering or categorising sources within the bundle.`},KnowledgeConfig:{auto_capture:`Automatically capture knowledge from conversations. Default: false.`,db_path:`Path to the knowledge graph SQLite database.`,enabled:`Enable the knowledge graph tool. Default: false.`,max_nodes:`Maximum number of knowledge nodes. Default: 100000.`,suggest_on_query:`Proactively suggest relevant knowledge on queries. Default: true.`},LambdaAiModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},LarkConfig:{ack_reactions:"Override for the top-level `ack_reactions` setting. When `None`, the\nchannel falls back to `[channels].ack_reactions`. When set\nexplicitly, it takes precedence.",app_id:`App ID from Lark/Feishu developer console`,app_secret:`App Secret from Lark/Feishu developer console`,approval_timeout_secs:`Time in seconds an approval card waits for user response before
the runtime auto-denies. Default: 300 (5 minutes).`,draft_update_interval_ms:`Minimum interval between consecutive \`update_draft\` PATCH calls in
milliseconds. Default 1000 ms tunes to Feishu's 5 QPS-per-message
edit cap; raise on enterprise plans with higher quotas.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",encrypt_key:`Encrypt key for webhook message decryption (optional)`,excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,mention_only:`When true, only respond to messages that @-mention the bot in groups.
Direct messages are always processed.`,per_user_session:`When \`true\`, group-chat sessions key on the sender's open_id, so
distinct members of the same group chat don't share conversation
context. When \`false\` (default), all members of a group share one
session keyed on chat_id (matches the existing behavior). 1-on-1
chats are unaffected (chat_id is already unique per user-bot pair).`,port:`HTTP port for webhook mode only. Must be set when receive_mode = "webhook".
Not required (and ignored) for websocket mode.`,proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",receive_mode:`Event receive mode: "websocket" (default) or "webhook"`,stream_mode:"Streaming mode for the LLM response: `off` (default) routes every\nresponse through `send()`; `partial` opens a Feishu interactive\ncard and edits it incrementally via `update_draft` /\n`finalize_draft`; `multi_message` is rejected for Lark (Feishu has\nno equivalent surface — falls back to `off` with a warning).",use_feishu:`Whether to use the Feishu (Chinese) endpoint instead of Lark (International)`,verification_token:`Verification token for webhook validation (optional)`},LeakDetectionConfig:{enabled:`Enable outbound credential leak detection and redaction.`,high_entropy_tokens:`Enable high-entropy token redaction; deterministic patterns still run when false.`,sensitivity:`Detection sensitivity from 0.0 to 1.0; higher is more aggressive.`},LeptonModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},LineConfig:{channel_access_token:`Long-lived channel access token (from LINE Developers Console).
Used for both the Reply API and the Push API fallback.
Falls back to the \`LINE_CHANNEL_ACCESS_TOKEN\` environment variable if empty.`,channel_secret:"Channel secret (from LINE Developers Console).\nUsed to verify the `X-Line-Signature` header on incoming webhooks.\nFalls back to the `LINE_CHANNEL_SECRET` environment variable if empty.",dm_policy:"DM (1:1 chat) access policy. Default: `pairing`.\n\n- `open`: respond to everyone\n- `pairing`: require one-time `/bind <code>` handshake on first contact\n- `allowlist`: respond only to user IDs listed in `allowed_users`",enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,group_policy:"Group / multi-person chat policy. Default: `mention`.\n\n- `open`: respond to every message\n- `mention`: respond only when @mentioned\n- `disabled`: ignore all group messages",proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",sender_name:'Display name shown as the sender in LINE chat bubbles.\nDefaults to `"AI"` when unset or empty.',webhook_port:"TCP port the embedded webhook server listens on. Default: `8443`."},LinkEnricherConfig:{enabled:`Enable the link enricher pipeline stage (default: false)`,max_links:`Maximum number of links to fetch per message (default: 3)`,timeout_secs:`Per-link fetch timeout in seconds (default: 10)`},LinkedInConfig:{api_version:`LinkedIn REST API version header (YYYYMM format).`,content:`Content strategy for automated posting.`,enabled:`Enable the LinkedIn tool.`,image:"Image generation for posts (`[linkedin.image]`)."},LinkedInContentConfig:{github_repos:"GitHub repositories to highlight (format: `owner/repo`).",github_users:`GitHub usernames whose public activity to reference.`,instructions:`Freeform posting instructions for the AI agent.`,persona:`Professional persona description (name, role, expertise).`,rss_feeds:`RSS feed URLs to monitor for topic inspiration (titles only).`,topics:`Topics of expertise and interest for post themes.`},LinkedInImageConfig:{card_accent_color:`Accent color for the fallback card (CSS hex).`,dalle:`OpenAI DALL-E model_provider settings.`,enabled:`Enable image generation for posts.`,fallback_card:`Generate a branded SVG text card when all AI model_providers fail.`,flux:`Flux (fal.ai) model_provider settings.`,imagen:`Google Imagen (Vertex AI) model_provider settings.`,providers:`ModelProvider priority order. Tried in sequence; first success wins.`,stability:`Stability AI model_provider settings.`,temp_dir:`Temp directory for generated images, relative to workspace.`},LinqConfig:{api_token:`Linq Partner API token (Bearer auth)`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,from_phone:`Phone number to send from (E.164 format)`,signing_secret:`Webhook signing secret for signature verification`},ListEntry:{enum_variants:"Variants for `enum`-kind fields — non-empty means the frontend should\nrender a `<select>` with these options. Empty for non-enum fields.",is_env_overridden:`Whether this field was populated by a \`ZEROCLAW_*\` env-var override
at load time. The dashboard renders the 💉 badge and a persistent
warning *"Edits here won't take effect — overridden by ZEROCLAW_..."*
when this is \`true\`.`,kind:"Stable kind tag — `string`, `bool`, `integer`, `float`, `enum`,\n`string-array`. Lowercase-kebab so it can be used directly as a CSS\nclass or React key.",multiline:"Surface hint from `#[multiline]`: render a multi-line text area\n(e.g. a PEM key body) instead of a single-line input.",section:"Onboard section name derived from the path's first segment via\n`Section::from_path`. `None` for paths that aren't part of any wizard\nsection. The dashboard groups list entries by this for per-section\nrendering — same source the CLI wizard uses, no schema attribute.",tab:"Tab grouping label from the field's `#[tab(...)]` annotation\n(`ConfigTab::label`). Absent for `ConfigTab::None`. Surfaces group\nlist entries into a tab bar by this; the agent edit form depends on\nit to split General / Providers / Channels / etc.",type_hint:"Rust type signature, e.g. `Option<String>`, `Vec<String>`, `u64`.\nRender in tooltips / hover state for the technically-curious."},ListResponse:{drifted:"Properties where in-memory and on-disk values disagree. Empty when the\ndaemon's view matches the file. Each entry follows the `DriftEntry`\nshape (secrets carry only `{path, secret: true, drifted: true}`)."},LitellmModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},LlamacppModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},LmstudioModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},LocalWhisperConfig:{bearer_token:`Bearer token for endpoint authentication.
Omit for unauthenticated local endpoints.`,max_audio_bytes:`Maximum audio file size in bytes accepted by this endpoint.
Defaults to 25 MB — matching the cloud API cap for a safe out-of-the-box
experience. Self-hosted endpoints can accept much larger files; raise this
as needed, but note that each transcription call clones the audio buffer
into a multipart payload, so peak memory per request is ~2× this value.`,timeout_secs:`Request timeout in seconds. Defaults to 300 (large files on local GPU).`,url:'HTTP or HTTPS endpoint URL, e.g. `"http://10.10.0.1:8001/v1/transcribe"`.'},LocalWhisperTranscriptionProviderConfig:{bearer_token:`Bearer token for endpoint authentication. Omit for unauthenticated
local endpoints.`,language:`Optional language hint (passed through to the local endpoint).`,max_audio_bytes:`Maximum audio file size in bytes accepted by this endpoint.
Defaults to 25 MB to match the cloud cap; raise as needed.`,timeout_secs:`Request timeout in seconds.`,uri:'Endpoint URL, e.g. `"http://10.10.0.1:8001/v1/transcribe"`.'},LucidStorageConfig:{binary_path:"Optional path to the lucid-memory binary. When unset, the bare `lucid`\nexecutable is resolved on `PATH`. A blank or whitespace-only value is\nrejected at config validation and remains invalid if startup continues,\nso an invalid explicit selector never falls through to `PATH`.",recall_timeout_ms:`Recall (context) timeout override, in milliseconds. Lucid CLI cold
starts (loading the local embedding model) can exceed 1.5s on ARM
hosts; raise this if recalls still time out on your hardware.
Unset falls back to the built-in default (3000ms). \`0\` is rejected
at config validation; because startup warns and continues after
validation errors, a \`0\` that reaches the runtime is treated as
unset and falls back to the default.`,store_timeout_ms:"Store timeout override, in milliseconds. Same cold-start\nconsideration as `recall_timeout_ms`. Unset falls back to the\nbuilt-in default (3000ms); `0` is rejected at validation and, if\nstartup continues after the warning, is likewise treated as unset."},ManifestModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},MarkdownStorageConfig:{directory:"Optional override for the markdown root directory.\nWhen unset, defaults to `<workspace_dir>/memory/`."},MatrixConfig:{access_token:"Matrix access token for the bot account. When unset, the channel\nfalls back to password login using `user_id` + `password`.",ack_reactions:"Override for the top-level `[channels].ack_reactions`. When\n`None`, falls back to the channels-wide default. When set\nexplicitly (`true`/`false`), takes precedence for this Matrix\ninstance only.",allowed_rooms:"Allowed Matrix room IDs. Empty = allow all rooms the bot has joined.\nEntries are matched literally against the canonical room ID\n(`!abc:server`) of each incoming message; `#room:server` aliases are\nnot resolved for this allowlist (they are resolved only for outbound\ndelivery targets such as cron `delivery.to`).",approval_timeout_secs:"Seconds to wait for operator approval on `always_ask` tools before auto-denying.",device_id:`Optional Matrix device ID.`,draft_update_interval_ms:`Minimum interval (ms) between draft message edits in Partial mode.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,homeserver:'Matrix homeserver URL (e.g. `"https://matrix.org"`).',interrupt_on_new_message:`Whether to interrupt an in-flight agent response when a new message arrives.`,mention_only:`When true, only respond to messages that @-mention the bot in groups.
Direct messages are always processed.`,multi_message_delay_ms:`Delay (ms) between sending each paragraph in MultiMessage mode.`,password:`Optional login password for Matrix account (used for initial login flow).`,recovery_key:`Optional Matrix recovery key for automatic E2EE key backup restore.
When set, ZeroClaw recovers room keys and cross-signing secrets on startup.`,reply_in_thread:`When true (default), replies are sent as thread replies. Starts a new thread from the
incoming message when none exists. When false, only continues existing threads.`,reply_min_interval_secs:"Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",reply_queue_depth_max:"Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",stream_mode:'Streaming mode for progressive response delivery.\n`"off"` (default): single message. `"partial"`: edit-in-place draft.\n`"multi_message"`: paragraph-split delivery.',user_id:'Optional Matrix user ID (e.g. `"@bot:matrix.org"`).'},MattermostConfig:{bot_token:"Mattermost bot access token. When unset, the channel falls back to\nthe login flow using `login_id` + `password`.",channel_ids:`Channel IDs to restrict the bot to. Empty or \`["*"]\` = auto-discover
every channel the bot can read (public, private, DMs, group DMs) and
poll them all. Explicit IDs disable discovery and pin the bot to the
listed channels only. Migrated from the legacy \`channel_id\` singular
field.`,discover_dms:"When true (default), auto-discovery includes DM (`type=D`) and group\nDM (`type=G`) channels. Set false to restrict the bot to public and\nprivate team channels only. Has no effect when `channel_ids` lists\nexplicit IDs. Defaults to `true` at the call site via\n`discover_dms.unwrap_or(true)`.",enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,interrupt_on_new_message:`When true, a newer Mattermost message from the same sender in the same channel
cancels the in-flight request and starts a fresh response with preserved history.`,listen_mode:'Listen mode: `"polling"` (REST API every 3s, default) or `"websocket"`\n(persistent WebSocket connection to `/api/v4/websocket` for near-real-time\nevent delivery). WebSocket mode reduces server load and delivers events\nfaster but requires a WebSocket-capable Mattermost server (v4.0+).',login_id:"Login ID (email or username) for the password login flow. Used only\nwhen `bot_token` is unset; both `login_id` and `password` must be\nset together.",mention_only:`When true, only respond to messages that @-mention the bot. Other
messages in the channel are silently ignored. DM and group DM
channels always bypass this filter: a 1:1 (or small-group) direct
conversation has no ambient noise to gate against, so every message
is treated as addressed to the bot.`,password:"Account password for the login flow. Used only when `bot_token` is\nunset; both `login_id` and `password` must be set together.",proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",reply_min_interval_secs:"Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",reply_queue_depth_max:"Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",team_ids:"Team IDs to restrict auto-discovery to. Empty = discover across every\nteam the bot belongs to. Non-empty = only discover public/private\nchannels whose `team_id` is in this list. DMs and group DMs (which\nhave no team) are governed by `discover_dms` instead.",thread_replies:`When true (default), replies thread on the original post.
When false, replies go to the channel root.`,url:'Mattermost server URL (e.g. `"https://mattermost.example.com"`).'},McpBundleConfig:{exclude:`MCP server IDs removed from the grant. Deny wins: a name listed here is
excluded even if another referenced bundle includes it.`,servers:"MCP server IDs (`[mcp.servers].name`) granted by this bundle."},McpConfig:{deferred_loading:"Load MCP tool schemas on-demand via `tool_search` instead of eagerly\nincluding them in the LLM context window. When enabled, only tool names\nare listed in the system prompt; the LLM must call `tool_search` to fetch\nfull schemas before invoking a deferred tool.",enabled:`Enable MCP tool loading.`,servers:'Configured MCP servers. The `#[nested]` annotation makes the macro\nexpose this as a List section in `map_key_sections()`, so the\ndashboard\'s `+ Add MCP server` affordance and the `POST\n/api/config/map-key?path=mcp.servers&key=<name>` endpoint pick it\nup automatically (no hand-table on the gateway side).\n\n`#[natural_key = "name"]` opts the Vec into per-element property\nrouting (see `route_vec_path` and the `Configurable` derive\'s\n`#[natural_key]` arm). With it, `set_prop("mcp.servers.<name>.url",\n...)` and `get_prop("mcp.servers.<name>.transport")` resolve to\nthe matching element\'s own `set_prop` / `get_prop`, and the\n`mcp.servers` section behaves like a `HashMap<String,\nMcpServerConfig>` from the dashboard / TUI\'s point of view.\n`name` itself becomes read-only via `set_prop`; rename goes\nthrough `rename_map_key` which mutates the `name` field in place.'},McpServerConfig:{args:`Command arguments for stdio transport.`,command:`Executable to spawn for stdio transport.`,env:`Optional environment variables for stdio transport.`,headers:`Optional HTTP headers for HTTP/SSE transports. Treated as secret:
the values commonly carry Bearer tokens for the upstream MCP server.`,name:'Display name used as a tool prefix (`<server>__<tool>`). Filled in\nfrom the supplied `map_key` when the entry is created via\n`create_map_key("mcp.servers", "<name>")`; `#[serde(default)]` lets\nthe macro default-construct from `{}` before the name gets injected.',pinned_resources:`Resource URIs to read once at agent startup and inject into the system
prompt as untrusted, server-origin context. Each is read via
\`resources/read\` on this server; pins on a server that does not advertise
resources, or that the agent's tool policy denies, are skipped with a
warning. Read once per run (not refreshed; no subscriptions).`,tool_timeout_secs:`Optional per-call timeout in seconds (hard capped in validation).`,transport:`Transport type (default: stdio).`,url:`URL for HTTP/SSE transports.`},MdnsConfig:{announce_interval_secs:`How often this node re-broadcasts its presence, in seconds.`,enabled:`Enable mDNS local peer discovery.`,max_peers:`Maximum number of unauthenticated LAN peer hints retained in memory.`,node_name:`Human-readable node name advertised to LAN peers. Defaults to a stable
local fallback when unset.`,peer_ttl_secs:`Seconds after the last announcement before a peer is evicted.`},MediaPipelineConfig:{describe_images:`Add image descriptions when a vision-capable model is active.`,enabled:`Master toggle for the media pipeline (default: false).`,summarize_video:`Summarize video attachments (placeholder — requires external API).`,transcribe_audio:`Transcribe audio attachments using the configured transcription model_provider.`},MemoryConfig:{archive_after_days:`Move daily/session files to the archive directory after this many days. Keeps the hot working set small without deleting history.`,audit_enabled:`Enable audit logging of memory operations.`,audit_retention_days:`Retention period for audit entries in days (default: 30).`,auto_hydrate:`Auto-hydrate from MEMORY_SNAPSHOT.md when brain.db is missing`,auto_reindex_on_identity_change:"Automatically re-embed all memories in the background when a change of embedding provider/model/dimensions is detected at startup (after the stale vectors have been cleared). Costs one embedding API call per memory, so it's off by default — leave it off for large stores and run `zeroclaw memory reindex` explicitly instead.",auto_save:`Auto-save what *you* tell ZeroClaw into memory as conversation history — the agent's own replies are not saved. Turn off if you want memory to only hold things you explicitly record via the memory tool.`,backend:'Dotted reference to the active storage instance: `<backend>.<alias>`\n(e.g. `"sqlite.default"`, `"postgres.work"`). Resolves through\n`Config.storage.<backend>.<alias>` at runtime. Bare backend names\n(`"sqlite"`) are treated as `"<backend>.default"`. Set to `"none"` to\ndisable persistence entirely.',candidate_multiplier:`Candidate pool multiplier over the final recall limit before blend/rerank trimming.
Values must be in 1..=20; runtime also enforces a bounded candidate pool.`,chunk_max_tokens:`Max tokens per chunk for document splitting`,conflict_supersede_enabled:`Enable reversible supersede soft-hide machinery when wired.`,conflict_threshold:`Cosine similarity threshold for conflict detection (0.0–1.0).`,consolidation_extract_facts:`Also extract atomic durable facts from each consolidated turn and store
them as individual Core memories. Default off; the flip is sequenced in
a later phase. SQLite-only: enabling this requires the sqlite memory
backend globally and on every agent (validated at config load).`,conversation_retention_days:"Delete conversation rows older than this many days from the DB (sqlite backend only). Age is measured by `updated_at` (last write time). 0 = keep forever.",core_max_bytes:`Maximum Core bytes before budget compaction. 0 = unbounded.`,core_max_rows:`Maximum Core rows before budget compaction. 0 = unbounded.`,core_retention_days:"Delete core memory rows older than this many days from the DB. Age is measured by `created_at` (first-write time). Neither recall nor ordinary rewrites refresh `created_at` under the current SQLite upsert, so core retention is an absolute age limit from first write. Set this to a generously large window for durable core memories, or keep 0 = keep forever.",daily_max_rows:`Maximum Daily rows before budget compaction. 0 = unbounded.`,daily_retention_days:"Delete daily memory rows older than this many days from the DB. Age is measured by `updated_at` (last write time). 0 = keep forever.",dedup_action:`Action to take when a duplicate is detected.`,dedup_jaccard_threshold:`Jaccard threshold for text-only duplicate detection.`,dedup_on_write:`Enable write-time near-duplicate detection.`,default_namespace:`Default namespace for memory entries.`,embedding_api_key:"Optional API key for the embedding endpoint. When set, embedding calls use this key instead of inheriting one from the seed model provider — decoupling embeddings from the chat model. Use it when the chat model runs on a provider that carries no usable embedding credential (e.g. an OAuth-only provider) while embeddings keep hitting an `openai`/`custom:` endpoint with their own key. Leave unset to inherit the seed provider's key (backward-compatible default).",embedding_cache_size:`Max embedding cache entries before LRU eviction`,embedding_dimensions:`Vector width produced by the embedding model — must match the model's native dimension or vectors won't store correctly. Look up the number on the model_provider's model page.`,embedding_model:"Embedding model identifier — must match a model your chosen embedding model_provider serves (e.g. `text-embedding-3-small` for OpenAI). Changing this invalidates existing embeddings: the change is detected at startup and stale vectors are cleared automatically; run `zeroclaw memory reindex` to re-embed (or set `auto_reindex_on_identity_change`).",embedding_provider:"Source of embedding vectors for semantic search. `none` = keyword-only retrieval (no API calls, no vector cost); `openai` = OpenAI's embedding API; `custom:URL` = any OpenAI-compatible embedding endpoint (LiteLLM, local gateway, etc.).",evict_order:`Eviction ordering for budget compaction.`,fts_early_return_score:`Reserved (0.0-1.0): the FTS score above which recall would skip the
vector stage. Inert until the backend exposes distinct FTS and vector
operations; recall is a single hybrid call today, so this has no effect.`,hygiene_enabled:`Run the periodic hygiene pass that archives stale daily/session files and enforces retention windows. Leave on unless you want to manage cleanup yourself.`,importance_weight:`Importance weight used by the recall blend.`,keyword_weight:"How heavily BM25 (keyword) overlap counts when `search_mode = hybrid`. Raise toward 1.0 for exact-term matching; lower it when paraphrases should still score well.",min_relevance_score:`Minimum hybrid score (0.0–1.0) for a memory to be included in context.
Memories scoring below this threshold are dropped to prevent irrelevant
context from bleeding into conversations. Default: 0.4`,mmr_lambda:`MMR relevance-vs-diversity weight, where 1.0 means relevance-only.`,pin_min_importance:`Pin entries at or above this importance. >1.0 means disabled.`,pin_namespaces:`Namespaces protected from budget eviction.`,policy:`Memory policy configuration.`,purge_after_days:`Delete archived files permanently after this many days. Set high if you need long-term history; set low for privacy / disk-space reasons.`,recency_weight:`Recency weight used by the recall blend.`,rerank_enabled:`Enable the recall rerank stage: blend retrieval score with importance
and recency, collapse near-duplicate entries, then trim back to the
recall limit. The advanced strategy below runs when the candidate
count reaches \`rerank_threshold\`.`,rerank_strategy:`Advanced rerank strategy. Valid: "none", "mmr".`,rerank_threshold:`Minimum candidate count to trigger the advanced rerank strategy.`,response_cache_enabled:`Enable LLM response caching to avoid paying for duplicate prompts`,response_cache_hot_entries:`Max in-memory hot cache entries for the two-tier response cache (default: 256)`,response_cache_max_entries:`Max number of cached responses before LRU eviction (default: 5000)`,response_cache_ttl_minutes:`TTL in minutes for cached responses (default: 60)`,retrieval_stages:'Retrieval stages for per-agent recall. Only `"cache"` is active: it\nenables an in-process, per-handle hot cache over recall results and is\nomitted from the default so recall stays coherent across handles.\n`"fts"` and `"vector"` are reserved for when the backend exposes\ndistinct FTS and vector operations; recall is a single hybrid backend\ncall today, so those names have no effect (kept for forward compat).',search_mode:"How memories are retrieved: `bm25` = keyword-only (no embeddings, cheapest); `embedding` = vector similarity only (needs an embedding model_provider); `hybrid` = blended keyword + vector score using the weights above (most robust).",snapshot_enabled:`Enable periodic export of core memories to MEMORY_SNAPSHOT.md`,snapshot_on_hygiene:`Run snapshot during hygiene passes (heartbeat-driven)`,types:"Typed memory configuration (`[memory.types]` section).",vector_weight:"How heavily vector (semantic) similarity counts when `search_mode = hybrid`. Raise toward 1.0 to favor meaning-based matches; lower it to lean on keyword overlap instead."},MemoryPolicyConfig:{max_entries_per_category:`Maximum entries per category (0 = unlimited).`,max_entries_per_namespace:`Maximum entries per namespace (0 = unlimited).`,read_only_namespaces:`Namespaces that are read-only (writes are rejected).`,redact_categories:"Redaction categories applied when `redact_on_write` is true.",redact_on_write:`Redact configured secret/PII categories before persistence.`,retention_days_by_category:`Retention days by category (overrides global). Keys: "core", "daily", "conversation".`,threat_scan:`Content scan mode for durable memory writes: "off", "on", or "strict".`,threat_scan_load_time:`Re-scan stored entries at recall/read time and withhold flagged entries.`,threat_scan_on_hit:`Behavior when a write-time content scan matches: "reject" or
"block-on-read".`},MemoryTypesConfig:{enabled:`Assign a first-class MemoryKind to new consolidation writes.`},Microsoft365Config:{auth_flow:`Authentication flow: "client_credentials" or "device_code"`,client_id:`Azure AD application (client) ID`,client_secret:`Azure AD client secret (stored encrypted when secrets.encrypt = true)`,enabled:`Enable Microsoft 365 integration`,scopes:`OAuth scopes to request`,tenant_id:`Azure AD tenant ID`,token_cache_encrypted:`Encrypt the token cache file on disk`,user_id:`User principal name or "me" (for delegated flows)`},MigrateResponse:{backup_path:`Backup path written when migration ran; absent when the config was
already at the current schema version.`},MinimaxModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",oauth_client_id:`Override of MiniMax's published OAuth client_id. Most operators
should leave this unset; the runtime defaults to the
vendor-published client_id (same one MiniMax's own portal uses).`,oauth_refresh_token:`Long-lived OAuth refresh token issued by MiniMax. When set, the
runtime exchanges it for a short-lived access token at provider
construction time and uses that as the API credential. Operators
who prefer dashboard-generated long-lived API keys can leave this
unset and populate \`api_key\` directly.`,pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},MistralModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},MochatConfig:{api_token:`Mochat API token`,api_url:`Mochat API base URL`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,poll_interval_secs:`Poll interval in seconds for new messages. Default: 5`},ModelCostRates:{cached_input_per_mtok:`Cached input tokens (USD per 1M). Optional — leave unset on
providers that don't charge separately for prompt cache hits.`,input_per_mtok:`Input tokens (USD per 1M).`,output_per_mtok:`Output tokens (USD per 1M).`},ModelRouteConfig:{api_key:`Optional API key override for this route's model provider`,hint:'Task hint name (e.g. "reasoning", "fast", "code", "summarize")\n`#[serde(default)]` lets the `create_map_key` macro default-construct\nfrom `{}` before the hint gets injected via `set_prop`. Empty strings\nare rejected by `Config::validate()`.',model:"Provider-local model identifier to use with that provider profile\n`#[serde(default)]` is required for `Default` + `create_map_key` construction.\nEmpty strings are rejected by `Config::validate()`.",model_provider:"Dotted provider profile ref to route to (must resolve to `providers.models.<type>.<alias>`)\n`#[serde(default)]` is required for `Default` + `create_map_key` construction.\nEmpty strings are rejected by `Config::validate()`."},MoonshotModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,endpoint:'Required: pick `cn`, `intl`, or `code`. Defaults to `intl` when omitted\nto ease transition; operators on the China endpoint should set\n`endpoint = "cn"` explicitly.',extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},MorphModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},MqttConfig:{broker_url:"MQTT broker URL (e.g., `mqtt://localhost:1883` or `mqtts://broker.example.com:8883`).\nUse `mqtt://` for plain connections or `mqtts://` for TLS.",client_id:`MQTT client ID (must be unique per broker).`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,keep_alive_secs:`Keep-alive interval in seconds (default: 30). Prevents broker disconnect on idle.`,password:`Password for authentication (optional).`,qos:`MQTT QoS level (0 = at-most-once, 1 = at-least-once, 2 = exactly-once). Default: 1.`,topics:"Topics to subscribe to (e.g., `sensors/#`, `alerts/+/critical`).\nAt least one topic is required.",use_tls:"Enable TLS encryption. Must match the broker_url scheme:\n- `mqtt://` → `use_tls: false`\n- `mqtts://` → `use_tls: true`",username:`Username for authentication (optional).`},MultimodalConfig:{allow_remote_fetch:`Allow fetching remote image URLs (http/https). Disabled by default.`,max_image_size_mb:`Maximum image payload size in MiB before base64 encoding.`,max_image_turns:`Maximum age of images in conversation turns.

When non-zero, images in user messages that are more than this many
turns back from the end of history are stripped before the request is
sent to the provider. This prevents a single screenshot from being
re-encoded and re-uploaded on every subsequent turn indefinitely.
Tool-result images are already managed by the stale-tool-result
mechanism and are not affected by this setting.

\`0\` (the default) disables age-based trimming entirely — images are
only evicted by the \`max_images\` count cap.`,max_images:`Maximum number of image attachments accepted per request.

Caps the total number of \`[IMAGE:...]\` markers that survive into the
provider request after multimodal preprocessing. Older images are
dropped first when the cumulative count exceeds this limit. Acts as
the upper bound on per-turn upload cost when tool outputs surface
local image paths.`,vision_model:'Model to use when routing to the vision model_provider (e.g. `"llava:7b"`).\nOnly used when `vision_model_provider` is set.',vision_model_provider:'ModelProvider name to use for vision/image messages (e.g. `"ollama"`).\nWhen set, messages containing `[IMAGE:]` markers are routed to this\nmodel_provider instead of the default text model_provider.'},NearaiModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},NebiusModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},NevisConfig:{client_id:`OAuth2 client ID registered in Nevis.`,client_secret:`OAuth2 client secret. Encrypted via SecretStore when stored on disk.`,enabled:`Enable Nevis IAM integration. Defaults to false for backward compatibility.`,instance_url:"Base URL of the Nevis instance (e.g. `https://nevis.example.com`).",jwks_url:`JWKS endpoint URL for local token validation.`,realm:`Nevis realm to authenticate against.`,require_mfa:`Require MFA verification for all Nevis-authenticated requests.`,role_mapping:`Nevis role to ZeroClaw permission mappings.`,session_timeout_secs:`Session timeout in seconds.`,token_validation:'Token validation strategy: `"local"` (JWKS) or `"remote"` (introspection).'},NevisRoleMappingConfig:{nevis_role:`Nevis role name (case-insensitive).`,workspace_access:'Workspace names this role can access. Use `"all"` for unrestricted.',zeroclaw_permissions:'Tool names this role can access. Use `"all"` for unrestricted tool access.'},NextcloudTalkConfig:{app_token:`Deprecated, unused. Nextcloud Talk sends do not authenticate via
OCS bearer auth (see \`webhook_secret\`); this field is only accepted so
existing configs that set it don't fail to parse. Safe to remove
from your config.`,base_url:'Nextcloud base URL (e.g. `"https://cloud.example.com"`).',bot_name:`Display name of the bot in Nextcloud Talk (e.g. "zeroclaw").
Used to filter out the bot's own messages and prevent feedback loops.
If not set, defaults to an empty string (no self-message filtering by name).`,bot_token:"DEPRECATED alias for `webhook_secret`, kept for migration.\n\nNextcloud issues ONE secret per installed bot and uses it for both\ndirections, so this cannot hold a different outbound secret. When both\nare set to different non-empty values the channel logs the conflict and\nfails closed as unconfigured: inbound `401` and no outbound send.\nPrefer `webhook_secret`; this alias will be removed.\n\nUpgrade from a `bot_token`-only configuration: copy the installed bot\nsecret to `webhook_secret`, verify replies still send, then delete\n`bot_token`.",draft_update_interval_ms:`Retained for configuration compatibility. Currently inert while draft
updates are disabled for this channel. Default: 1000 ms.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",stream_mode:`Retained for configuration compatibility. Nextcloud Talk's bot API does
not provide message IDs or edit/delete operations, so draft updates are
disabled and responses are currently sent as one final message for every
value.`,webhook_secret:`The bot secret Nextcloud installed for this bot. Canonical field.

Used for BOTH directions: verifying inbound webhook signatures and
signing outbound bot-API requests. When it is unset, inbound webhooks
are rejected and no outbound request is sent (fail closed).

Can also be set via \`ZEROCLAW_NEXTCLOUD_TALK_WEBHOOK_SECRET\`.`},NgrokTunnelConfig:{auth_token:`ngrok auth token`,domain:`Optional custom domain`},NodeRunOverlay:{tool_calls:`Tool invocations captured while the step executed. Empty for
unreached steps and runs recorded before capture landed.`},NodeTransportConfig:{allowed_peers:`Allow specific node IPs/CIDRs.`,connection_pool_size:`Maximum number of connections per peer.`,enabled:`Enable the secure transport layer.`,max_request_age_secs:`Maximum age of signed requests in seconds (replay protection).`,mutual_tls:`Require client certificates (mutual TLS).`,require_https:`Require HTTPS for all node communication.`,shared_secret:`Shared secret for HMAC authentication between nodes.`,tls_cert_path:`Path to TLS certificate file.`,tls_key_path:`Path to TLS private key file.`},NodesConfig:{auth_token:`Optional bearer token for node authentication.`,enabled:`Enable dynamic node discovery endpoint.`,max_nodes:`Maximum number of concurrent node connections.`,mdns:`LAN-local mDNS peer discovery.`},NostrConfig:{enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,private_key:`Private key in hex or nsec bech32 format`,relays:`Relay URLs (wss://). Defaults to popular public relays if omitted.`},NovitaModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},NscaleModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},NvidiaModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},ObservabilityConfig:{backend:`Observability sink: none | log | verbose | prometheus | otel.`,log_llm_request_payload:'LLM request payload capture: "off" | "redacted" | "full".\n- `off` (default): only `messages_count` lands on the `llm_request`\n  event. No prompt or conversation content is persisted.\n- `redacted`: the full message history (role + content) is leak-scanned\n  and truncated at `log_tool_io_truncate_bytes` before persisting.\n- `full`: full message history, still leak-scanned, untruncated. For\n  operators who need replay fidelity and accept the disk cost.\n\nOpt-in (default off) because `full`/`redacted` capture the entire system\nprompt and conversation on every turn.',log_persistence:`Log persistence mode: "none" | "rolling" | "full".
Controls whether every event passing through \`zeroclaw_log::record!\`
is appended to the on-disk JSONL log.`,log_persistence_max_bytes:'Size threshold in bytes that triggers an archive rotation when\n`log_persistence = "rotating"`. The active file is rotated once an\nappend leaves it at or above this size. `0` disables size-based\nrotation. Ignored unless `log_persistence = "rotating"`.',log_persistence_max_entries:'Maximum entries retained when `log_persistence = "rolling"`.',log_persistence_path:`Log persistence file path. Relative paths resolve under workspace_dir.`,log_persistence_retention_max_age_days:'Retention cap on the age (in days) of rotated archive files when\n`log_persistence = "rotating"`. After a rotation, archives older than\nthis many days are deleted. `0` disables age-based cleanup. Ignored\nunless `log_persistence = "rotating"`.',log_persistence_retention_max_files:'Retention cap on the number of rotated archive files kept alongside the\nactive file when `log_persistence = "rotating"`. After a rotation the\noldest archives beyond this count are deleted. `0` keeps all archives.\nIgnored unless `log_persistence = "rotating"`.',log_persistence_rotate_daily:'Rotate the active file to an archive on a UTC day boundary when\n`log_persistence = "rotating"`: before the first event of a new day is\nappended, a file whose last write fell on an earlier day is archived.\nIgnored unless `log_persistence = "rotating"`.',log_tool_io:'Tool I/O capture policy: "off" | "redacted" | "full".\n- `off`: only tool name + outcome + duration land in the log.\n- `redacted` (default): tool input + output are leak-scanned and\n  truncated at `log_tool_io_truncate_bytes` before persisting.\n- `full`: full input + output, still leak-scanned. For operators\n  who need replay fidelity and accept the disk cost.',log_tool_io_denylist:`Tool names whose I/O is never logged beyond name + outcome + duration
regardless of \`log_tool_io\`. Use for tools whose I/O is intrinsically
sensitive (e.g. memory recall against personal namespaces, agent
secret reads). Empty by default.`,log_tool_io_truncate_bytes:'Truncate the captured tool input and output at this many bytes when\n`log_tool_io = "redacted"`. Truncated events carry an explicit\n`tool_output_truncated: true` flag plus `tool_output_original_bytes`.',otel_endpoint:'OTLP endpoint (e.g. `"http://localhost:4318"`). Only used when backend = `"otel"`.',otel_genai_content:'OTel GenAI content capture: "off" | "redacted" | "full".\nControls whether `gen_ai.system_instructions`, `gen_ai.input.messages`,\nand `gen_ai.output.messages` are emitted on OTel spans.\n- `off` (default): no content attributes, only metadata.\n- `redacted`: content is leak-scanned and truncated at `otel_genai_content_max_chars`.\n- `full`: content is leak-scanned but not truncated.',otel_genai_content_max_chars:'Per-field character truncation limit for OTel GenAI content when\n`otel_genai_content = "redacted"`. Each string field is truncated\nindependently. `0` is treated as `off`.',otel_headers:'Optional HTTP headers sent with every OTLP export request (e.g. authorization).\nSpecified as key-value pairs in TOML:\n```toml\n[observability.otel_headers]\nAuthorization = "Bearer sk-..."\n```',otel_service_name:`Service name reported to the OTel collector. Defaults to "zeroclaw".`,otel_tool_io:'OTel tool I/O capture: "off" | "redacted" | "full".\nControls whether `gen_ai.tool.arguments`, `input.value`,\n`gen_ai.tool.result`, and `output.value` are emitted on OTel spans.\n- `off` (default): no content attributes, only tool name + outcome.\n- `redacted`: content is leak-scanned and truncated at `otel_tool_io_max_chars`.\n- `full`: content is leak-scanned but not truncated.',otel_tool_io_max_chars:'Per-field character truncation limit for OTel tool I/O when\n`otel_tool_io = "redacted"`. Each string field is truncated\nindependently. `0` is treated as `off`.'},OllamaModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",num_ctx:"Override the Ollama `num_ctx` (context window, in tokens) sent on\nevery `/api/chat` request. Defaults to the framework constant\n(`OLLAMA_DEFAULT_NUM_CTX`) when unset.",num_predict:"Override the Ollama `num_predict` (max output tokens) sent on every\n`/api/chat` request. Defaults to the framework constant\n(`OLLAMA_DEFAULT_NUM_PREDICT`) when unset.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,temperature_override:"Force every Ollama `/api/chat` request to use this temperature,\noverriding the per-call value passed through\n`ModelProvider::chat_with_system(.., temperature)`. When unset\n(`None`, the default), the per-call temperature wins: full\nbackward compatibility.",think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},OnboardStateConfig:{completed_sections:'Section keys the user has completed at least once.\nValues are the lowercased Section variant names\n(`"workspace"`, `"model_providers"`, …).',quickstart_completed:"`true` once the Quickstart has applied a `BuilderSubmission`\nsuccessfully on this install. Web gateway and TUI auto-launch\nthe Quickstart on startup iff this is `false` **and** no\n`agents.*` entries exist (the implicit-completion rule covers\nupgrades). The flag is flipped in the same atomic write that\nlands the Quickstart submission; re-entering the Quickstart\nlater to add another agent does not flip it back to `false`."},OpenAIModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},OpenAITtsProviderConfig:{api_key:`API key (openai, elevenlabs, google).`,binary_path:`Path to backend binary (edge-tts subprocess; piper local server).`,language_code:"Language code for google (e.g. `en-US`).",model:"Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",response_format:'Audio response format sent to the TTS backend (e.g. `"opus"`, `"mp3"`,\n`"wav"`). Defaults to `"opus"` for the OpenAI family. Override to\n`"wav"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `"mp3"` for broader compatibility.',similarity_boost:"Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",speed:"Playback speed multiplier (openai only; default `1.0`).",stability:"Voice stability for elevenlabs (0.0-1.0; default `0.5`).",uri:"Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",voice:"Voice override for this instance. When empty, falls back to\n`[tts].default_voice`."},OpenAiSttConfig:{api_key:`OpenAI API key for Whisper transcription.`,model:`Whisper model name (default: "whisper-1").`},OpenAiTranscriptionProviderConfig:{api_key:`API key for the transcription provider.`,initial_prompt:`Whisper-style initial prompt to bias the model toward expected
vocabulary (proper nouns, technical terms). Provider-specific support;
silently ignored where not applicable.`,language:'Optional language hint passed to the provider (ISO-639-1 like `"en"` /\n`"ru"`, or BCP-47 like `"en-US"` for Google). Most providers auto-detect\nwhen this is unset.',model:'Whisper model name (default: `"whisper-1"`).'},OpenCodeCliConfig:{enabled:"Enable the `opencode_cli` tool",env_passthrough:`Extra env vars passed to the opencode subprocess`,max_output_bytes:`Maximum output size in bytes (2MB default)`,timeout_secs:`Maximum execution time in seconds (coding tasks can be long)`},OpenRouterModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},OpenVpnTunnelConfig:{advertise_address:'Advertised address once VPN is connected (e.g., `"10.8.0.2:42617"`).\nWhen omitted the tunnel falls back to `http://{local_host}:{local_port}`.',auth_file:"Optional path to auth credentials file (`--auth-user-pass`).",config_file:"Path to `.ovpn` configuration file (must not be empty).",connect_timeout_secs:`Connection timeout in seconds (default: 30, must be > 0).`,extra_args:`Extra openvpn CLI arguments forwarded verbatim.`},OpencodeModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},OsaurusModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},OtpConfig:{cache_valid_secs:`Reuse window for recently validated OTP codes.`,challenge_max_attempts:`Maximum number of OTP challenge attempts before lockout.`,enabled:`Enable OTP gating. Defaults to disabled for backward compatibility.`,gated_actions:`Tool/action names gated by OTP. Empty or malformed entries are rejected
at config load; an entry that does not match a known gated action is
accepted but logged at WARN, since it cannot be enforced.`,gated_domain_categories:"Domain-category presets expanded into `gated_domains`.",gated_domains:`Explicit domain patterns gated by OTP.`,method:`OTP method.`,token_ttl_secs:`TOTP time-step in seconds.`},OvhModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},PacingConfig:{loop_detection_enabled:"Enable pattern-based loop detection (exact repeat, ping-pong,\nno-progress). Defaults to `true`.",loop_detection_max_repeats:`Number of consecutive identical tool+args calls before the first
escalation (Warning). Defaults to 3.`,loop_detection_min_elapsed_secs:`Minimum elapsed seconds before loop detection activates.
Tasks completing under this threshold get aggressive loop protection;
longer-running tasks receive a grace period before the detector starts
counting. \`None\` means loop detection is always active (existing behavior).`,loop_detection_window_size:`Sliding window size for the pattern-based loop detector.
Defaults to 20.`,loop_ignore_tools:`Tool names excluded from identical-output / alternating-pattern loop
detection. Useful for browser workflows where \`browser_screenshot\`
structurally resembles a loop even when making progress.`,message_timeout_scale_max:`Override for the hardcoded timeout scaling cap (default: 4).
The channel message timeout budget is computed as:
  \`message_timeout_secs * min(max_tool_iterations, message_timeout_scale_max)\`
Raising this value lets long multi-step tasks with slow local models
receive a proportionally larger budget without inflating the base timeout.`,step_timeout_secs:`Per-step timeout in seconds: the maximum time allowed for a single
LLM inference turn, independent of the total message budget.
\`None\` means no per-step timeout (existing behavior).`},PairingDashboardConfig:{code_length:`Length of pairing codes (default: 8)`,code_ttl_secs:`Time-to-live for pending pairing codes in seconds (default: 3600)`,lockout_secs:`Lockout duration in seconds after max attempts (default: 300)`,max_failed_attempts:`Maximum failed pairing attempts before lockout (default: 5)`,max_pending_codes:`Maximum concurrent pending pairing codes (default: 3)`},PatchOpResult:{comment:`Comment that was applied alongside this op (if any). Echoed so
clients can confirm the comment was actually written to disk
without having to round-trip through \`GET\` and parse the TOML.`,value:"The resulting value at the target path after the op applied.\n`None` for secret paths (per the secrets-handling boundary), and for\n`remove` ops where the field was reset to its default."},PatchResponse:{warnings:`Non-fatal validation warnings against the saved configuration.`},PeerGroupConfig:{admin_for_agent_scope:"When `true`, members of this peer group are authorized to issue\n`/model --agent <model>` on the agent this group is bound to.\nDefault `false` (deny-by-default). The runtime resolves this live\nfrom `Config::peer_groups` at command-dispatch time via\n`Config::channel_agent_scope_admins`; no cache, no per-channel\nduplicate sender list.",agents:`Member agents by alias.`,channel:'Either a channel type (`"telegram"`) or a dotted channel alias\n(`"telegram.work"`). A bare type applies to every alias of that\ntype; a dotted form scopes the group to that single instance.',external_peers:`Non-agent members by channel-native username.`,ignore:`Per-group blocklist; subtracts from the resolved peer set.`,output_modality:"Preferred output modality for all peers in this group.\nDefaults to `mirror` (input-driven). Set to `voice` to have the\nagent always reply and deliver proactive messages (cron, announces)\nas TTS voice notes on channels that support audio output."},PeripheralBoardConfig:{baud:`Baud rate for serial (default: 115200)`,board:`Board type: "nucleo-f401re", "rpi-gpio", "esp32", etc.`,path:`Path for serial: "/dev/ttyACM0", "/dev/ttyUSB0"`,transport:`Transport: "serial", "native", "websocket"`},PeripheralsConfig:{boards:`Board configurations (nucleo-f401re, rpi-gpio, etc.)`,datasheet_dir:`Path to datasheet docs (relative to workspace) for RAG retrieval.
Place .md/.txt files named by board (e.g. nucleo-f401re.md, rpi-gpio.md).`,enabled:`Enable peripheral support (boards become agent tools)`},PerplexityModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},PinggyTunnelConfig:{region:'Server region: `"us"` (USA), `"eu"` (Europe), `"ap"` (Asia), `"br"` (South America), `"au"` (Australia), or omit for auto.',token:`Pinggy access token (optional — free tier works without one).`},PipelineConfig:{allowed_tools:`Tools allowed in pipeline steps. Steps referencing tools not on this
list are rejected before execution.`,enabled:"Enable the `execute_pipeline` meta-tool.\nDefault: `false`.",max_steps:"Maximum number of steps allowed in a single pipeline invocation.\nDefault: `20`."},PiperTtsProviderConfig:{api_key:`API key (openai, elevenlabs, google).`,binary_path:`Path to backend binary (edge-tts subprocess; piper local server).`,language_code:"Language code for google (e.g. `en-US`).",model:"Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",response_format:'Audio response format sent to the TTS backend (e.g. `"opus"`, `"mp3"`,\n`"wav"`). Defaults to `"opus"` for the OpenAI family. Override to\n`"wav"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `"mp3"` for broader compatibility.',similarity_boost:"Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",speed:"Playback speed multiplier (openai only; default `1.0`).",stability:"Voice stability for elevenlabs (0.0-1.0; default `0.5`).",uri:"Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",voice:"Voice override for this instance. When empty, falls back to\n`[tts].default_voice`."},PlannedToolCall:{args:`Argument template; string leaves may contain bindings.`,pinned:`Pinned sample output for authoring/preview.`},PluginLimitsConfig:{call_fuel:`Fuel budget per plugin call (wasmtime instruction units).`,max_instances:`Maximum component instances a plugin store may create.`,max_memory_mb:`Maximum linear memory a plugin store may grow to, in megabytes.`,max_table_elements:`Maximum table elements a plugin store may allocate.`},PluginSecurityConfig:{signature_mode:`Signature enforcement mode: "disabled", "permissive", or "strict".`,trusted_publisher_keys:`Hex-encoded Ed25519 public keys of trusted plugin publishers.`},PluginsConfig:{auto_discover:`Auto-discover and load plugins on startup`,enabled:`Enable the plugin system (default: false)`,limits:`Per-call WASM execution limits`,max_plugins:`Maximum number of plugins that can be loaded`,plugins_dir:`Directory where plugins are stored`,security:`Plugin signature verification security settings`},PostgresStorageConfig:{connect_timeout_secs:`Optional connection timeout in seconds.`,db_url:'Connection URL (e.g. `"postgres://user:pass@host/db"`).\nAccepts legacy aliases: dbURL, database_url, databaseUrl.',schema:`Database schema for the memory table.`,table:`Table name for memory entries.`,vector_dimensions:`Vector dimensions for pgvector embeddings.`,vector_enabled:`Enable pgvector extension for hybrid vector+keyword recall.`},ProjectIntelConfig:{default_language:`Default report language (en, de, fr, it). Default: "en".`,enabled:`Enable the project_intel tool. Default: false.`,include_git_data:`Include git log data in reports. Default: true.`,include_jira_data:`Include Jira data in reports. Default: false.`,jira_base_url:`Jira instance base URL (required if include_jira_data is true).`,report_output_dir:`Output directory for generated reports.`,risk_sensitivity:`Risk detection sensitivity: low, medium, high. Default: "medium".`,templates_dir:`Optional custom templates directory.`},PropResponse:{warnings:`Non-fatal validation warnings against the current config state.
On GET this surfaces warnings present in the loaded config; on PUT
this surfaces warnings against the post-save state. Empty when
nothing is flagged.`},ProviderCostRates:{models:"`[cost.rates.providers.models.<type>.<model>]`.",transcription:"`[cost.rates.providers.transcription.<type>.<model>]`.",tts:"`[cost.rates.providers.tts.<type>.<voice>]`."},Providers:{models:"Model providers — `[providers.models.<type>.<alias>]`.",transcription:"Transcription / speech-to-text providers — `[providers.transcription.<type>.<alias>]`.",tts:"Text-to-speech providers — `[providers.tts.<type>.<alias>]`."},ProxyConfig:{all_proxy:`Fallback proxy URL for all schemes.`,enabled:`Enable proxy support for selected scope.`,http_proxy:`Proxy URL for HTTP requests (supports http, https, socks5, socks5h).`,https_proxy:`Proxy URL for HTTPS requests (supports http, https, socks5, socks5h).`,no_proxy:`No-proxy bypass list. Same format as NO_PROXY.`,scope:`Proxy application scope.`,services:`Service selectors used when scope = "services".`},QQConfig:{app_id:`App ID from QQ Bot developer console`,app_secret:`App Secret from QQ Bot developer console`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only."},QdrantStorageConfig:{api_key:"API key for Qdrant Cloud or secured instances.\nUse `ZEROCLAW_storage__qdrant__<alias>__api_key=...` for env injection.",collection:"Collection name for storing memories.\nUse `ZEROCLAW_storage__qdrant__<alias>__collection=...` for env injection.",url:'Qdrant server URL (e.g. `"http://localhost:6333"`).\nUse `ZEROCLAW_storage__qdrant__<alias>__url=...` for env injection.'},QianfanModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},QueryClassificationConfig:{enabled:"Enable automatic query classification. Default: `false`.",rules:`Classification rules evaluated in priority order.`},QwenModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,auth_mode:"Auth flow. Defaults to `api_key`; set to `oauth` to use the vendor's\nOAuth-cache integration instead of the `api_key` field.",chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",oauth_client_id:`Override of Qwen's published OAuth client_id. Most operators
should leave this unset.`,oauth_refresh_token:`Long-lived Qwen OAuth refresh token. When set, the runtime
exchanges it for a short-lived access token at provider
construction time. Operators relying on the upstream \`qwen login\`
tool (which writes \`~/.qwen/oauth_creds.json\`) leave this unset;
the file-cache integration takes over.`,oauth_resource_url:"Operator override of the resource URL the refreshed access token\nis paired with. When unset, the runtime falls back to the\n`endpoint`-derived URL (or the cached `resource_url` when reading\nfrom `~/.qwen/oauth_creds.json`).",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},RedditConfig:{client_id:`Reddit OAuth2 client ID.`,client_secret:`Reddit OAuth2 client secret.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,refresh_token:`Reddit OAuth2 refresh token for persistent access.`,subreddits:"Subreddits to filter messages (without `r/` prefix). Empty = accept\nfrom any subreddit the bot has access to. Migrated from the legacy\n`subreddit` singular field.",username:"Reddit bot username (without `u/` prefix)."},RekaModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},ReliabilityConfig:{api_keys:"Additional API keys for round-robin rotation on rate-limit (429) errors.\nThe primary `api_key` is always tried first; these are extras.",channel_initial_backoff_secs:`Initial backoff for channel/daemon restarts.`,channel_max_backoff_secs:`Max backoff for channel/daemon restarts.`,provider_backoff_ms:`Base backoff (ms) for model_provider retry delay.`,provider_retries:`Retries per model_provider before bailing.`,scheduler_poll_secs:`Scheduler polling cadence in seconds.`,scheduler_retries:`Max retries for cron job execution attempts.`},ReloadStatusResponse:{pending_reload:`Whether any config write has landed since the last admin reload and may
still require subsystem re-instantiation to take effect.`},RiskProfileConfig:{allowed_commands:`Allowlist of executable names for shell execution.`,allowed_roots:`Extra directory roots the agent may access.`,allowed_tools:'Tools the agent may call in agentic mode. Empty = inherit / no\nauthorization constraint. Authorization decision: which tools is\nthe agent permitted to invoke at all. See `excluded_tools` for\nthe inverse denylist scoped to non-CLI channels.\n\nThe TOML config does not distinguish an omitted field from\n`allowed_tools = []`; both deserialize to `Vec::new()` and\n`SecurityPolicy::from_profiles` maps that to "no authorization\nconstraint" at this layer. If you need an explicit deny-all gate,\napply it on the caller-supplied per-run `allowed_tools` (cron\njobs and other narrowers pass that list in directly to\n`ToolAccessPolicy`, which honors `Some(vec![])` as deny-all) or\nvia `excluded_tools` covering the specific tools you want blocked.\n\nMCP exception: when the list is non-empty, runtime-discovered MCP\ntools (any name containing `__`, which is the `<server>__<tool>`\nconvention used by the MCP wrapper) are auto-admitted into the\neffective allow-list without needing to be listed here individually.\nThis keeps the post-change eager-MCP default usable for agents with an\nexplicit allow-list. Block individual MCP tools via `excluded_tools`.\n\nScope of the exception: the `__` auto-admit applies only to this\nrisk-profile allow-list, **not** to caller-supplied per-run\n`allowed_tools` (cron job `allowed_tools`, narrowed delegate\ninvocations, etc.). Per-run lists are still strict explicit-list\nintersections, so a job that narrows `allowed_tools = ["cron_add"]`\nwill not see runtime-discovered MCP tools unless it names them.',always_ask:`Tools that always require approval in this profile.`,approval_route:`Route this profile's tool approvals to a DISTINCT approver channel instead of the
channel that triggered the run (closes the cross-channel-HITL gap). Absent ⇒ the
originating channel approves (today's behavior). See [\`crate::autonomy::ApprovalRoute\`].

Honored on both the interactive channel-driven path and the non-interactive turn path
(gateway chat/webhook dispatch and agent-to-agent peer messages). On the non-interactive
path the approver is resolved from the live daemon channel registry; if no registry is
available or the approver is not live, the gate keeps the non-interactive default
(fail-closed deny under the default \`on_no_approver\`).`,auto_approve:`Tools that never require approval in this profile.`,block_high_risk_commands:`Block high-risk commands even when allowlisted.`,delegation_policy:`Whether agents using this profile may initiate delegation. Defaults to
\`Forbidden\`. Reachable delegate targets and bounded/independent mode
are resolved from each caller agent's config.`,excluded_tools:`Tools excluded from non-CLI channels under this profile.

Also subtracts from the agentic-delegate allow-list resolved at
runtime, which is the only way to block individual
\`<server>__<tool>\` MCP names that would otherwise be auto-admitted
by the \`allowed_tools\` MCP exception described above.`,firejail_args:`Extra arguments forwarded to firejail when sandbox_backend = "firejail".`,forbidden_paths:`Explicit path denylist.`,level:"Autonomy level applied to this profile. Default: `supervised`.",require_approval_for_medium_risk:`Require approval for medium-risk operations.`,sandbox_backend:'Sandbox backend identifier (e.g. `"firejail"`, `"landlock"`). `None` inherits.',sandbox_enabled:"Whether the sandbox is enabled for this profile. `None` inherits global.",shell_env_passthrough:`Environment variable names passed through to shell subprocesses.`,workspace_only:"Restrict filesystem access to workspace-relative paths. Default: `false`."},RuntimeConfig:{docker:'Docker runtime settings (used when `kind = "docker"`).',kind:`Runtime kind: native | docker | cloudflare.`,reasoning_effort:`Optional reasoning effort for model_providers that expose a level control.`,reasoning_enabled:"Global reasoning override for model_providers that expose explicit controls.\n- `None`: model_provider default behavior\n- `Some(true)`: request reasoning/thinking when supported\n- `Some(false)`: disable reasoning/thinking when supported",shell:'Shell binary the native runtime uses for command execution.\n\nApplies only to `runtime.kind = "native"`; other runtimes ignore it.\nWhen unset or `null`, the system default `sh` is used. The shell is\ninvoked as `<shell> -c "<command>"`, so it must be a POSIX-compatible\nshell binary.\n\nAccepted forms (Unix):\n- a bare command name resolved via `PATH` (e.g. `"bash"`), or\n- an absolute path (e.g. `"/bin/bash"`, `"/usr/bin/zsh"`).\n\nThe value is validated when the native runtime is constructed, so a bad\nvalue is reported up front rather than failing on the first shell\ncommand. Rejected: empty/whitespace; a relative path with separators\n(e.g. `"./sh"`, `"bin/sh"` — use a bare `PATH` name or an absolute path\ninstead); a bare name not found on `PATH`; and a path that does not\nexist or is not executable.\n\n**Ignored on Windows and Android** (and not validated there): Windows\nalways uses `cmd.exe`, and Android always uses `/system/bin/sh`\n(its shell is not on `PATH` for spawned processes).\n\n**Examples:**\n```toml\n[runtime]\nshell = "bash" # resolves via PATH\nshell = "/bin/zsh" # absolute path\n```'},RuntimeProfileConfig:{agentic:`Enable agentic (multi-turn tool-call loop) mode.`,agentic_timeout_secs:"Agentic delegate run timeout in seconds. `None` inherits global.",compact_context:"Use compact bootstrap (6000 chars / 2 RAG chunks). `None` inherits.",delegation_timeout_secs:"Delegate call timeout in seconds. `None` inherits global delegate timeout.",keep_tool_context_turns:"Number of recent turns whose full tool context is preserved. `None` inherits.",max_actions_per_hour:"Maximum actions allowed per hour. `0` is a hard zero budget — the\nper-sender rate tracker treats a max of 0 as always exhausted\n(`PerSenderTracker::is_exhausted`), blocking every action. For an\neffectively-unlimited budget use a high value (e.g. `u32::MAX`), not 0.\n`SecurityPolicy::ensure_no_escalation_beyond` rejects subagents\nthat try to raise this above the parent's value.",max_context_tokens:"Maximum estimated tokens for context before compaction. `None` inherits.",max_cost_per_day_cents:"Maximum cost per day in cents. `0` inherits the global limit.\nParent-subset enforced for subagents.",max_delegation_depth:"Maximum delegation recursion depth. `0` inherits the default.",max_history_messages:"Maximum conversation history messages retained per session. `None` inherits.",max_system_prompt_chars:"Maximum characters for the assembled system prompt. `None` inherits.",max_tool_iterations:"Maximum tool-call iterations in agentic mode. `0` inherits the global default.",max_tool_result_chars:"Maximum characters for a single tool result. `None` inherits.",memory_recall_limit:"Maximum memory entries injected per turn. `None` inherits global default (5).\nSet to `0` for unlimited.",parallel_tools:"Enable parallel tool execution per iteration. `None` inherits.",prompt_injection_mode:"How skills are injected into the system prompt for agents on this\nprofile. `None` inherits the global `[skills] prompt_injection_mode`;\n`compact` inlines only compact skill metadata and registers the\n`read_skill` tool, `full` inlines full skill instructions. Resolved\nthrough [`Config::effective_skills_prompt_mode`], the single point both\nthe system-prompt builder and the `read_skill` tool-registration gate\nconsult so they always agree on the effective mode.",shell_timeout_secs:"Shell subprocess timeout in seconds. `0` inherits the global timeout.\nParent-subset enforced for subagents.",tool_call_dedup_exempt:`Tools exempt from within-turn dedup check.`,tool_dispatcher:'Tool dispatch strategy (e.g. `"auto"`). `None` inherits.'},SambanovaModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},SchedulerConfig:{catch_up_on_startup:`Run all overdue jobs at scheduler startup. Default: \`true\`.

When the daemon restarts late, jobs whose \`next_run\` is in the past
fire once before normal polling resumes. Disable to wait for the
next scheduled occurrence instead.`,enabled:`Enable the built-in scheduler loop. When false, no cron jobs run.`,max_concurrent:`Maximum tasks executed in parallel within a single polling cycle.`,max_run_history:"Maximum number of historical cron run records to retain. Default: `50`.",max_tasks:`Maximum number of persisted scheduled tasks per polling cycle.`},SecretsConfig:{encrypt:`Enable encryption for API keys and tokens at rest`},SecurityConfig:{audit:`Audit logging configuration`,estop:`Emergency-stop state machine configuration.`,leak_detection:"Outbound credential leak detection and redaction configuration. See\n`[security.leak_detection]` for the detector controls.",nevis:`Nevis IAM integration for SSO/MFA authentication and role-based access.`,otp:`OTP gating configuration for sensitive actions/domains.`,webauthn:`WebAuthn / FIDO2 hardware key authentication configuration.`},SecurityOpsConfig:{auto_triage:`Automatically triage incoming alerts without user prompt.`,enabled:`Enable security operations tools.`,max_auto_severity:`Maximum severity level that can be auto-remediated without approval.
One of: "low", "medium", "high", "critical". Default: "low".`,playbooks_dir:`Directory containing incident response playbook definitions (JSON).`,report_output_dir:`Directory for generated security reports.`,require_approval_for_actions:`Require human approval before executing playbook actions.`,siem_integration:`Optional SIEM webhook URL for alert ingestion.`},SglangModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},ShellToolConfig:{timeout_secs:`Maximum shell command execution time in seconds (default: 60).`},SignalConfig:{account:`E.164 phone number of the signal-cli account (e.g. "+1234567890").`,approval_timeout_secs:"Seconds to wait for operator approval on `always_ask` tools before auto-denying.",dm_only:'When true, only accept direct messages and ignore all group traffic.\nMutually exclusive with `group_ids` (which is ignored when this is\nset). Migrated from the legacy `group_id = "dm"` sentinel.',enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,group_ids:"Group IDs to filter messages. Empty = accept all messages (DMs and\ngroups). When non-empty, only messages from listed groups are\naccepted (DMs are still accepted unless `dm_only` flips the policy\nto DMs-only). Migrated from the legacy `group_id` singular field.",http_url:'Base URL for the signal-cli HTTP daemon (e.g. `"http://127.0.0.1:8686"`).',ignore_attachments:`Skip messages that are attachment-only (no text body).`,ignore_stories:`Skip incoming story messages.`,proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",reply_min_interval_secs:"Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",reply_queue_depth_max:"Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged."},SiliconflowModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},SkillBundleConfig:{directory:`Directory path (relative to workspace root) to load skills from.`,exclude:`Skill names to exclude from this bundle.`,include:"Skill names to include. Empty means include all skills in `directory`."},SkillCreationConfig:{enabled:"Enable automatic skill creation after successful multi-step tasks.\nDefault: `false`.",max_final_answer_chars:`Maximum characters of the final assistant answer fed into the
reflection prompt. Bounds prompt size so a long answer cannot blow up
the reflection request. Default: \`2000\`.`,max_skills:`Maximum number of auto-generated skills to keep.
When exceeded, the oldest auto-generated skill is removed (LRU eviction).`,max_task_chars:"Maximum characters of the task description fed into the reflection\nprompt. Default: `1000`.",max_tool_trace_chars:"Maximum characters of the rendered tool-call trace fed into the\nreflection prompt. Default: `4000`.",reflection_enabled:"Synthesize a canonical `SKILL.md` from the execution trace via a\nbounded model-provider reflection call instead of the deterministic\n`SKILL.toml` generator. Requires `enabled = true`. Falls back to\n`SKILL.toml` whenever the reflection call or its output is invalid, so\nturning this on never leaves a skill un-created. This is distinct from\nthe `[skills.skill_improvement]` background review fork, which patches\nexisting skills after use rather than creating new ones from a trace.\nDefault: `false`.",similarity_threshold:`Embedding similarity threshold for deduplication.
Skills with descriptions more similar than this value are skipped.`},SkillImprovementConfig:{cooldown_secs:"Minimum interval (in seconds) between reviews for the same skill.\nActs as a durable rate limit on patches to any one skill. Default: `3600`.",enabled:`Enable the background skill-review fork. Default: \`false\`.
Opt-in: users must explicitly enable this to allow post-turn skill
mutations.`,max_review_iterations:"Maximum tool-call iterations the review fork itself is allowed to make.\nCaps the fork's LLM cost. Default: `8`.",nudge_interval_iterations:"Spawn a review fork once at least this many tool-call iterations have\naccumulated in the current run. `0` disables iteration-based triggering.\nDefault: `10`."},SkillInstallSuggestionsConfig:{enabled:"Enable suggestions for installable skills before normal agent turns.\nDefault: `false`."},SkillsConfig:{allow_scripts:"Allow script-like files in skills (`.sh`, `.bash`, `.ps1`, shebang shell files).\nDefault: `false` (secure by default).",extra_registries:"Additional user-configured skill registries, installed via\n`registry:<name>/<skill>`. Each reuses the git-clone registry path and\nis cloned to its own `extra-registry-<name>/` workspace directory.",install_suggestions:`Prompt-triggered install suggestions for missing skills.`,open_skills_dir:"Optional path to a local open-skills repository.\nIf unset, defaults to `$HOME/open-skills` when enabled.",open_skills_enabled:"Enable loading and syncing the community open-skills repository.\nDefault: `false` (opt-in).",prompt_injection_mode:"Controls how skills are injected into the system prompt.\n`full` preserves legacy behavior. `compact` keeps context small and loads skills on demand.",registry_url:"URL of the skills registry repository for bare-name installs.\nDefault: `https://github.com/zeroclaw-labs/zeroclaw-skills`",skill_creation:`Autonomous skill creation from successful multi-step task executions.`,skill_improvement:`Automatic skill self-improvement after successful skill usage.`},SlackConfig:{app_token:"Slack app-level token for Socket Mode (xapp-...). When unset or empty,\nresolved at channel construction from `ZEROCLAW_SLACK_APP_TOKEN`, then\n`SLACK_APP_TOKEN`. `#[serde(default)]` makes omission explicit (an\n`Option` field is already omittable, but this keeps it consistent with\n`bot_token`).",approval_timeout_secs:"Seconds to wait for operator approval on `always_ask` tools before auto-denying.",bot_token:"Slack bot OAuth token (xoxb-...). Optional in config: when unset or\nempty it is resolved at channel construction from\n`ZEROCLAW_SLACK_BOT_TOKEN`, then `SLACK_BOT_TOKEN`. `#[serde(default)]`\nso a config that omits it still deserializes - the env fallback then\nsupplies it - instead of failing with `missing field 'bot_token'`.",cancel_reaction:'Emoji reaction name (without colons) that cancels an in-flight request.\nFor example, `"x"` means reacting with `:x:` cancels the task.\nLeave unset to disable reaction-based cancellation.',channel_ids:`Explicit list of channel IDs to watch.
Empty = listen across all accessible channels.
Migrated from the legacy \`channel_id\` singular field.`,draft_update_interval_ms:`Minimum interval (ms) between draft message edits to avoid Slack rate limits.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,interrupt_on_new_message:`When true, a newer Slack message from the same sender in the same channel
cancels the in-flight request and starts a fresh response with preserved history.`,mention_only:`When true, only respond to messages that @-mention the bot in groups.
Direct messages remain allowed.`,proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",reply_min_interval_secs:"Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",reply_queue_depth_max:"Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",stream_drafts:"Enable progressive draft message streaming via `chat.update`.",strict_mention_in_thread:`When true (and \`mention_only\` is also true), messages inside a Slack
thread must also @-mention the bot to trigger a response. By default,
thread replies are allowed through without a mention so the bot can
keep a back-and-forth going without the user repeating @-mentions.
Set this to true in channels shared with human discussion where the
bot should stay silent unless explicitly addressed.`,thread_replies:`When true (default), replies stay in the originating Slack thread.
When false, replies go to the channel root instead.`,use_markdown_blocks:"Use the newer Slack `markdown` block type (12 000 char limit, richer formatting).\nDefaults to false (uses universally supported `section` blocks with `mrkdwn`).\nEnable this only if your Slack workspace supports the `markdown` block type."},Sop:{admission_policy:"How to handle a trigger that cannot be admitted immediately because this\nSOP's execution slots are full (A2). Default `parallel`.",agent:"Parent agent alias that owns this procedure. Every `execute` step runs\nas this agent unless the step names its own `agent` override. Required\nfor headless triggers (mqtt, webhook, cron, amqp), which have no\nambient agent loop to borrow.",cooldown_secs:"Minimum seconds between successive runs of this procedure. `0`\n(default) disables the cooldown and back-to-back runs are allowed.",description:`Free-text summary of what the procedure does and when it fires. Shown
in the SOP list and header; purely descriptive, never executed.`,deterministic:`When true, sets execution_mode to Deterministic.
Steps execute sequentially without LLM round-trips.`,execution_mode:"How steps are driven: `auto`, `supervised` (default), `step_by_step`,\n`priority_based`, or `deterministic`. `deterministic = true` forces\nthe last regardless of this field.",max_concurrent:`Maximum simultaneous runs of this one procedure. Excess trigger
firings queue or drop per the engine's concurrency policy. Default 1.`,max_pending_approvals:"Upper bound on runs of this SOP parked at a HITL approval at once\n(`0` = unlimited). Once reached, further triggers are deferred (surfaced for\nbackpressure/redelivery) rather than silently dropped. Default `0`.",name:`Unique procedure name. Doubles as the on-disk directory key, so a
rename in the editor moves the SOP's folder. Must be non-empty.`,priority:"Scheduling priority when concurrency limits force a choice between\nrunnable procedures: `critical`, `high`, `normal` (default), `low`.",steps:`Ordered step definitions. This is the body of the procedure; the
daemon renumbers and remaps routing refs on save.`,triggers:`Signals that start a run. A procedure may bind several triggers; any
one firing (subject to its own condition) launches the SOP.`,version:"Semantic version string for the procedure definition (e.g. `1.0.0`).\nBump it when you change step behavior so runs are traceable."},SopApprovalConfig:{groups:"Named approver groups: `group name -> members`. A member is matched against\nthe transport-derived (channel-authenticated) `ApprovalPrincipal` identity.\nA member may be source-qualified (`<source>:<identity>`, e.g. `http:ZeroClawOperator`,\n`ws:<subject>`, `agent:<alias>`) to grant rights on one transport only, or a\nbare identity (`alice`) to grant from any non-channel source. Channel members\nmust include the channel namespace (`channel:<channel-key>:<sender>`) so sender\nids from different channel aliases cannot collide. A future auth system adds a\nsecond resolver alongside this one; it does not replace channel identities.",policies:`Named approval policies a SOP step may reference by name.`},SopConfig:{approval:`Approval broker policy config (\`[sop.approval]\`): named approver groups and
per-name approval policies (required group + quorum + escalation route) the
approval broker consumes for group-membership and quorum checks. Members are
channel-provided identities (a gateway user, a forge login), so this is a
permanent identity source, not a stopgap; a future auth system adds another
resolver alongside it rather than replacing it. An empty block means no broker
policy applies (\`approval_mode\` alone governs a gate, unchanged behavior).`,approval_mode:`WHO may clear a SOP approval gate. Layered with execution_mode / priority /
requires_confirmation (those still apply). Default \`both\` keeps today's
behavior (the agent tool OR an out-of-band principal can clear a gate).`,approval_timeout_action:"What happens to a SOP gate that times out (after `approval_timeout_secs`).\nDefault `escalate` is fail-closed: re-surface to the out-of-band approver and\nkeep waiting, never self-approve. (The SOP-gate analog of the channel\napproval-routing fail-closed default; reconcile with that model if both land.)",approval_timeout_secs:"Approval timeout in seconds. When a run waits for approval longer than\nthis, the configured `approval_timeout_action` is applied (default\n`escalate`: re-surface the gate to the out-of-band approver and never\nself-approve). Set to 0 to disable the timeout sweep.",default_execution_mode:"Default execution mode for SOPs that omit `execution_mode`.\nValues: `auto`, `supervised` (default), `step_by_step`,\n`priority_based`, `deterministic`.",maintenance_interval_secs:"How often (seconds) the daemon runs the SOP maintenance tick: fire\nfail-closed approval timeouts (per `approval_timeout_secs` /\n`approval_timeout_action`), reap expired concurrency-claim leases, and\nprune terminal runs past `max_finished_runs`. Default `60`; set to `0` to\ndisable the tick entirely. The tick itself self-approves nothing - timeout\nhandling is governed by `approval_timeout_action` (default `escalate`).",max_concurrent_total:`Maximum total concurrent SOP runs across all SOPs.`,max_finished_runs:`Maximum number of finished runs kept in memory for status queries.
Oldest runs are evicted when over capacity. 0 = unlimited.`,max_step_retries:`Maximum retries allowed by a step failure policy.`,max_step_visits:`Maximum times a routed SOP run can visit one step.`,persist_runs:"Persist run state durably across restarts. Default `true`: `build_sop_engine`\nselects the configured backend (`sqlite`) and in-flight runs - including runs\nparked at a HITL approval - survive a restart. This is the durable substrate\nthe HITL admission model relies on so a pending approval is not lost when the\ndaemon restarts. Set to `false` to opt back into ephemeral in-memory state.",procedural_memory_enabled:`Enable SOP procedural-memory proposal tooling. Default false keeps
self-modifying SOP write-back opt-in while the SOP subsystem is
Experimental.`,run_state_dir:"Directory for the durable run store (created mode-0700). When omitted,\n`<data_dir>/sop`. Never OS-temp.",run_store_backend:"Durable run-state backend when `persist_runs` is true: `sqlite` (default,\ndurable) or `memory` (explicitly non-durable, for tests/degraded).",sops_dir:`Directory containing SOP definitions (subdirs with SOP.toml + SOP.md).
Optional override. When omitted, the runtime and CLI both resolve the
default \`<workspace>/sops\`; SOPs load from there whenever it exists.`,step_mandatory_tools:`Tool names that remain available while step scope is enforced.`,step_schema_enforce:`Enforce per-step input/output schemas when a step declares them.`,step_scope_enforce:"Enforce per-step tool scope. Default false keeps `tools:` advisory.",untrusted_frame_warning:`Include the explanatory warning text inside untrusted-content frames.
Boundary framing itself is always on once wired.`,untrusted_guard_sensitivity:`Prompt-guard and outbound-redaction sensitivity for untrusted SOP content.`,untrusted_input_guard:`Prompt-guard action for untrusted SOP trigger input: warn, block, or sanitize.`,untrusted_outbound_redact:`Redact outbound SOP content before persistence/audit consumers write it.
Intended to converge with the shared B/F redaction switch once those consumers land.`,untrusted_payload_max_bytes:`Maximum bytes accepted from untrusted SOP trigger payload/topic content
before char-boundary truncation. 0 disables the cap.`},SopStep:{agent:`Agent alias that runs this step. Overrides the SOP's parent agent when
set; unset inherits the parent. Authored, persisted, and resolved into
the executing action via \`effective_agent\` (step override then parent).
Spawning a distinct per-step agent's own session at execution time is
staged follow-on work; today the resolved alias is stamped on the action
and the active agent loop executes the step body.`,body:`The step's instruction body in Markdown. This is the prompt the
running agent executes for the step; bindings like \`{{steps.N}}\`
resolve against prior step outputs at run time.`,calls:"Ordered tool calls planned for this step. Args may carry\n`{{steps.N}}` / `{{calls.K}}` bindings validated at save time.",capability:'Capability identifier used when `kind = "capability"`.',edit:`Editable-field opt-in for a checkpoint step (\`- edit: body\` bullet): the
named field of the piped value an approver may amend before the run
resumes (the gate prompt gains an "Edit" choice). Absent = no editing.`,gate_prompt:`Authored gate-notice template for a HITL step (\`- prompt:\` bullet).
\`{{path.to.field}}\` placeholders resolve against the step's piped input
(the trigger payload at step 1, the previous step's output later) — pure
lookups, no logic. Rendered into the out-of-band approval notice so the
approver sees WHAT they are approving; absent = an automatic summary.`,kind:"Step kind: `execute` (default) or `checkpoint`.",mode:`Optional per-step execution mode override.`,number:`Ordinal position of this step within the procedure. Steps run in
number order unless routing overrides the sequence; the daemon
renumbers on save so gaps and reorders normalize to 1..N.`,on_failure:`Failure handling metadata. Default preserves fail-the-run behavior.`,policy:"Approval policy name (a key in `[sop.approval].policies`) the approval broker\nenforces for this step's gate: required approver group + quorum. `None` keeps\ntoday's behavior (`approval_mode` alone governs, no membership/quorum).",pos:`Persisted canvas coordinate, set by dragging the node in the Blueprint
editor. Absent until the node is moved; not surfaced in the step form.`,requires_confirmation:"Pause for human confirmation before this step runs. Independent of\n`checkpoint` kind and the SOP-level approval gate; both still apply.",routing:`Conditional routing metadata. Default preserves linear execution.`,schema:`Typed input/output schemas for deterministic data flow validation.`,scope:"Tool scope for this step. `suggested_tools` remains the legacy alias.",suggested_tools:"Advisory tool names surfaced to the agent for this step. Legacy alias\nfor `scope.allow`; when `step_scope_enforce` is off these are hints,\nnot a hard restriction.",title:`Short human label for the step, shown on the canvas node and in the
step list. Leave blank and the surface falls back to "untitled".`,with:"Capability arguments, serialized as `with` in TOML/JSON definitions."},SopTrigger:{alias:`Optional configured-instance alias; unset matches every instance.`,board:`Board identifier the signal originates from.`,calendar_ids:`Calendar IDs to scope to; empty matches all of the source's calendars.`,calendar_source:`Calendar source identifier the event originates from.`,channel:"`ChannelKind` snake_case value naming the channel type.",condition:`Optional expression evaluated against the message payload; the run
starts only when it holds.`,events:`Change kinds to match; empty matches every kind.`,expression:`Cron expression evaluated over the run window.`,path:`Request path matched exactly against the event path.`,routing_key:"Routing-key filter (topic-exchange semantics): `.`-delimited words,\n`*` matches one word, `#` matches zero or more words.",signal:"Signal name on the board; matched as `board/signal`.",topic:"Topic filter. `+` matches one level, `#` matches the remaining levels."},SqliteStorageConfig:{open_timeout_secs:"Maximum seconds to wait when opening the DB if it's locked.\n`None` waits indefinitely. Recommended max: 300.",path:"Optional override for the SQLite database path.\nWhen unset, defaults to `<workspace_dir>/brain.db`."},StepRouting:{depends_on:`Step numbers that must have completed before this step can run.`,next:`Explicit successor step number.`,switch:`Ordered switch ports. Non-empty makes this a multi-branch switch node:
each port is a named conditional out-edge, matched top to bottom.`,terminal:`When true, this step ends its branch: no implicit fallthrough to the
following step is derived. Lets an authoring surface delete the default
sequence edge and leave a node free-floating between saves.`,when:`Guard evaluated against accumulated run data.`},StepSchema:{input:`JSON Schema object describing expected input shape.`,output:`JSON Schema object describing expected output shape.`},StepToolCall:{args:`Arguments the tool actually received.`,duration_ms:`Wall-clock duration in milliseconds.`,index:`Zero-based invocation order within the step.`,output:`Display text of the tool output.`,output_data:"Structured output when the tool declared one (`ToolOutput::data`)."},StepToolScope:{allow:`Only these names or groups are allowed when present.`,deny:`These names or groups are always subtracted.`},StepfunModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},StorageConfig:{lucid:"Lucid CLI sync instances (`[storage.lucid.<alias>]`).",markdown:"Markdown storage instances (`[storage.markdown.<alias>]`).",postgres:"PostgreSQL storage instances (`[storage.postgres.<alias>]`).",qdrant:"Qdrant storage instances (`[storage.qdrant.<alias>]`).",sqlite:"SQLite storage instances (`[storage.sqlite.<alias>]`)."},SwitchRule:{goto:`Target step this port routes to.`,name:"Port label shown on the node's output pin (e.g. `pull_request`).",when:"Guard evaluated against accumulated run data. `None` = catch-all."},SyntheticModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},TailscaleTunnelConfig:{funnel:`Use Tailscale Funnel (public internet) vs Serve (tailnet only)`,hostname:`Optional hostname override`},TelegramConfig:{ack_reactions:"Override for the top-level `ack_reactions` setting. When `None`, the\nchannel falls back to `[channels].ack_reactions`. When set\nexplicitly, it takes precedence.",api_base_url:`Telegram Bot API base URL. Defaults to the official Telegram endpoint;
set to a local Bot API server URL when self-hosting Telegram's bot API.`,approval_timeout_secs:`How long (seconds) to wait for the operator to tap an inline-keyboard
button on a tool approval prompt before auto-denying. Default: 120.`,bot_token:"Telegram Bot API token (from @BotFather). `#[serde(default)]` so a\nconfig that omits or later has it pruned (e.g. a freshly created\nalias with an empty token, stripped by `prune_empty_leaves` before\nwrite) still deserializes as an empty string - instead of failing\nwith `missing field 'bot_token'` and getting dropped by the resilient\nsalvage pass. `validate_bot_token` below still requires a real token\nonce `enabled = true`.",debounce_ms:"Inbound message debounce window in milliseconds for this Telegram alias.\nWhen set, overrides the global `[channels].debounce_ms` for this channel\nonly. `0` or unset falls back to the global value.",draft_update_interval_ms:`Minimum interval (ms) between draft message edits to avoid rate limits.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,interrupt_on_new_message:`When true, a newer Telegram message from the same sender in the same chat
cancels the in-flight request and starts a fresh response with preserved history.`,mention_only:`When true, only respond to messages that @-mention the bot in groups.
Direct messages are always processed.`,proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",reply_min_interval_secs:"Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",reply_queue_depth_max:"Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",stream_mode:`Streaming mode for progressive response delivery via message edits.`},TelnyxModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},TextBrowserConfig:{allowed_private_hosts:'Private/internal hosts allowed to bypass SSRF protection.\nExact and subdomain matches are supported; `["*"]` permits **all** private/local\nhosts (RFC 1918, loopback, link-local, `.local`). Default: empty (deny).\nWarning: `["*"]` also reaches link-local addresses, including the cloud metadata\nendpoint (`169.254.169.254`) — list specific hosts unless you accept that exposure.',enabled:"Enable `text_browser` tool",preferred_browser:`Preferred text browser ("lynx", "links", or "w3m"). If unset, auto-detects.`,timeout_secs:`Request timeout in seconds (default: 30)`},ThinkingConfig:{native_thinking:`Enables provider-native thinking parameters when the selected level has a budget.`},TodoTrackerConfig:{enabled:"Master switch. When `false` the tracker never renders and never\nauto-pops. Default: `true`.",enabled_at_start:"Whether the panel is visible at launch (when `enabled`). When\n`false` it stays hidden until toggled or auto-popped by the first\nplan. Default: `false`.",location:"Panel location: `bottom` (between transcript and input), `left`,\nor `right` (default).",max_height:"Bottom-strip maximum height in rows (grows up to this). Ignored for\nleft/right. Default: `5`.",width:"Side-panel target column width (left/right). Runtime-clamped to at\nmost half the terminal width. Ignored for `bottom`. Default: `32`."},TogetherModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},ToolCostRates:{per_call:`Per-call cost (USD).`},ToolFilterGroup:{keywords:'Keywords that activate this group in `dynamic` mode (case-insensitive substring).\nIgnored when `mode = "always"`.',mode:'Activation mode: `"always"` or `"dynamic"`.',tools:"Glob patterns matching MCP tool names (single `*` wildcard supported)."},ToolReceiptsConfig:{enabled:`Generate HMAC receipts on every tool execution. Default: \`false\`.
When false, the entire receipt subsystem is inert (no key, no
generation, no append, no system-prompt addendum).`,inject_system_prompt:"Inject the receipt-echo instruction into the system prompt so the\nmodel carries receipts verbatim into its response. Default: `true`.\nNo effect when `enabled = false`.",show_in_response:"Append a trailing `Tool receipts:` block to user-visible replies so\nreceipts are auditable from the channel surface, not just the\ninternal history. Default: `false`."},TranscriptionConfig:{api_key:`API key used for transcription requests (Groq transcription provider).

Use the schema-mirror env grammar (\`ZEROCLAW_transcription__api_key=...\`)
for runtime env injection. Legacy \`GROQ_API_KEY\` constructor fallback was
removed in v0.8.0.`,api_url:`Whisper API endpoint URL (Groq transcription provider).`,assemblyai:`AssemblyAI STT model_provider configuration.`,deepgram:`Deepgram STT model_provider configuration.`,enabled:`Enable voice transcription for channels that support it.`,google:`Google Cloud Speech-to-Text model_provider configuration.`,initial_prompt:`Optional initial prompt to bias transcription toward expected vocabulary
(proper nouns, technical terms, etc.). Sent as the \`prompt\` field in the
Whisper API request.`,language:`Optional language hint (ISO-639-1, e.g. "en", "ru") for Groq transcription provider.`,local_whisper:`Local/self-hosted Whisper-compatible STT model_provider.`,max_audio_bytes:`Optional global audio size upper bound in bytes, enforced before
dispatching to any transcription provider. Provider-specific caps still
apply.`,max_duration_secs:`Maximum voice duration in seconds (messages longer than this are skipped).`,model:`Whisper model name (Groq transcription provider).`,openai:`OpenAI Whisper STT model_provider configuration.`,transcribe_non_ptt_audio:"Also transcribe non-PTT (forwarded/regular) audio messages on WhatsApp,\nnot just voice notes. Default: `false` (preserves legacy behavior)."},TranscriptionCostRates:{per_minute:`Audio transcribed (USD per minute).`},TriggerSourceRegistry:{operators:"The comparison operators every condition builder renders. Read from\n[`crate::sop::condition::ConditionOp`], never hand-listed by a surface."},TtsConfig:{default_format:'Default audio output format (`"mp3"`, `"opus"`, `"wav"`).',default_voice:`Default voice ID passed to the selected tts provider.`,enabled:`Enable TTS synthesis.`,max_text_length:`Maximum input text length in characters (default 4096).`},TtsCostRates:{per_mchar:`Characters synthesised (USD per 1M).`},TunnelConfig:{cloudflare:'Cloudflare Tunnel configuration (used when `tunnel_provider = "cloudflare"`).',custom:'Custom tunnel command configuration (used when `tunnel_provider = "custom"`).',ngrok:'ngrok tunnel configuration (used when `tunnel_provider = "ngrok"`).',openvpn:'OpenVPN tunnel configuration (used when `tunnel_provider = "openvpn"`).',pinggy:'Pinggy tunnel configuration (used when `tunnel_provider = "pinggy"`).',tailscale:'Tailscale Funnel/Serve configuration (used when `tunnel_provider = "tailscale"`).',tunnel_provider:"How the gateway gets exposed to the public internet so webhooks (Telegram, Slack, etc.) can reach it. `none` = keep it local, no tunnel; `cloudflare` = Cloudflare Tunnel via cloudflared (needs a Zero Trust account and token); `tailscale` = Tailscale Funnel/Serve (tailnet-only or public, no account beyond tailscale); `ngrok` = ngrok agent with auth token; `openvpn` = bring-your-own OpenVPN egress; `pinggy` = Pinggy SSH tunnels (quick one-shot URLs); `custom` = run an arbitrary command you define under `[tunnel.custom]`."},TwitchConfig:{bot_username:`Twitch login name of the bot account (case-insensitive — lowercased
before send).`,channels:'Twitch channels to join. Each entry receives a `#` prefix if missing\nand is lowercased before send (Twitch channel names are\ncase-insensitive). E.g. `["mychannel", "#anotherchannel"]`.',enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false`.",mention_only:"When true, only respond to messages that mention the bot's login\nname. Default: `false`.",oauth_token:'Twitch OAuth user-access token. The `oauth:` prefix is added\nautomatically if missing, so both `"oauth:abcdef"` and `"abcdef"`\nwork. Mint via <https://twitchapps.com/tmi/> or the Twitch CLI.'},TwitterConfig:{bearer_token:`Twitter API v2 Bearer Token (OAuth 2.0)`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`},UpgradeRequest:{auto_restart:`After a successful swap, exit so a supervisor relaunches the new binary.
Only honoured under a detected supervisor (systemd/launchd).`,version:`Target release tag; defaults to latest.`},UpgradeStatusResponse:{phase:"0 before the first `Phase N/6` marker, else 1..=6."},UpstageModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},ValidationWarning:{code:`Stable machine-readable identifier for the warning class.`,message:`Human-readable description suitable for direct display.`,path:'Dotted property path the warning concerns\n(e.g. `"agents.researcher.model_provider"`).'},VeniceModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},VercelModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},VerifiableIntentConfig:{enabled:`Enable VI credential verification on commerce tool calls (default: false).`,strictness:`Strictness mode for constraint evaluation: "strict" (fail-closed on unknown
constraint types) or "permissive" (skip unknown types with a warning).
Default: "strict".`},VersionCheckResponse:{error:`Set only when the check failed; absent on success.`,latest_version:"Latest release version, or `null` when the check could not complete.",release_notes:`Release notes body (Markdown).`},VllmModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},VoiceCallConfig:{enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`},VoiceDuplexConfig:{enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`},VoiceWakeConfig:{enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",energy_threshold:"RMS energy threshold for voice activity detection. Samples below\nthis level are treated as silence. Default: `0.01`.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,max_capture_secs:"Maximum capture duration in seconds before forcing transcription.\nDefault: `30`.",silence_timeout_ms:"Silence timeout in milliseconds: how long to wait after the last\nenergy spike before finalizing a capture window. Default: `2000`.",wake_word:'Wake word phrase to listen for (case-insensitive substring match).\nDefault: `"hey zeroclaw"`.'},WatiConfig:{api_token:`WATI API token (Bearer auth).`,api_url:`WATI API base URL (default: <https://live-mt-server.wati.io>).`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",tenant_id:`Tenant ID for multi-channel setups (optional).`},WeChatConfig:{api_base_url:"Override the iLink API base URL. Default: `https://ilinkai.weixin.qq.com`.",cdn_base_url:"Override the CDN base URL. Default: `https://novac2c.cdn.weixin.qq.com/c2c`.",enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,state_dir:"Directory to persist bot token and sync cursor.\nDefault: `~/.zeroclaw/wechat/`."},WeComConfig:{enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,webhook_key:`Webhook key from WeCom Bot configuration`},WeComWsConfig:{allowed_groups:`Allowed WeCom group chat IDs. Empty = deny all groups, "*" = allow all groups.`,allowed_users:`Allowed WeCom user IDs. Empty = deny all, "*" = allow all users.`,bot_id:`Bot ID for WeCom WebSocket subscription.`,bot_name:`Display name or mention alias of the WeCom AI bot, for example \`danya\`.

WeCom group text often arrives as plain text such as \`@danya say hi\`;
passing this name through lets the generic reply-intent precheck
recognize that a group message was addressed to the bot.`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,file_retention_days:`File retention days for downloaded WeCom attachments under the workspace cache.`,max_file_size_mb:`Maximum accepted file size in MiB for WeCom attachment download attempts.`,proxy_url:`Optional per-channel proxy override. Falls back to the global proxy config when empty.`,secret:`Secret for WeCom WebSocket subscription authentication.`,stream_mode:`Streaming mode for progressive draft delivery over the WeCom long connection.`},WebAuthnConfig:{enabled:`Enable WebAuthn authentication. Default: false.`,rp_id:`Relying Party ID (domain name, e.g. "example.com"). Default: "localhost".`,rp_name:`Relying Party display name. Default: "ZeroClaw".`,rp_origin:'Relying Party origin URL (e.g. `"https://example.com"`). Default: `"http://localhost:42617"`.'},WebFetchConfig:{allowed_domains:'Allowed domains for web fetch (exact or subdomain match; `["*"]` = all public hosts)',allowed_private_hosts:'Private/internal hosts allowed to bypass SSRF protection (e.g. `["192.168.1.10", "internal.local"]`).\nExact and subdomain matches are supported. Listing a host skips the\nresolved-IP SSRF check for it; a *literal* private/local host (IP literal,\nlocalhost, .local) listed here also bypasses `allowed_domains`. The `*`\nwildcard permits any literal private/local host and lets a name already in\n`allowed_domains` resolve to a private IP, but it does NOT widen\n`allowed_domains` — a non-private host (matched explicitly or via `*`) still\nneeds `allowed_domains`, so `*` cannot reach an arbitrary public host.',blocked_domains:`Blocked domains (exact or subdomain match; always takes priority over allowed_domains)`,enabled:"Enable `web_fetch` tool for fetching web page content",firecrawl:"Firecrawl fallback configuration (`[web_fetch.firecrawl]`)",max_response_size:`Maximum response size in bytes (default: 500KB, plain text is much smaller than raw HTML)`,timeout_secs:`Request timeout in seconds (default: 30)`},WebSearchConfig:{bocha_api_key:'Bocha AI Web Search API key (required if search_provider is `"bocha"`). Obtain at <https://open.bochaai.com>.',brave_api_key:`Brave Search API key (required if search_provider is "brave")`,enabled:"Enable `web_search_tool` for web searches",jina_api_key:`Jina AI API key (required if search_provider is "jina")`,max_results:`Maximum results per search (1-10)`,search_provider:`Search provider: "duckduckgo" (free), "brave" (requires API key), "tavily" (requires API key), "searxng" (self-hosted), "jina" (requires API key), or "bocha" (Bocha AI, requires API key — Chinese-friendly, <https://open.bochaai.com>)`,searxng_instance_url:'SearXNG instance URL (required if search_provider is `"searxng"`), e.g. `"https://searx.example.com"`.',tavily_api_key:`Tavily Search API key (required if search_provider is "tavily")`,timeout_secs:`Request timeout in seconds`},WebhookAuditConfig:{enabled:"Enable the webhook-audit hook. Default: `false`.",include_args:`Include tool call arguments in the audit payload. Default: \`false\`.

Be mindful of sensitive data — arguments may contain secrets or PII.`,max_args_bytes:`Maximum size (in bytes) of serialised arguments included in a single
audit payload. Arguments exceeding this limit are truncated.
Default: \`4096\`.`,tool_patterns:'Glob patterns for tool names to audit (e.g. `["Bash", "Write"]`).\nAn empty list means **no** tools are audited.',url:`Target URL that will receive the audit POST requests.`},WebhookConfig:{auth_header:"Optional `Authorization` header value for outbound requests.",enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,listen_path:"URL path to listen on (default: `/webhook`).",max_retries:"Maximum number of retry attempts for outbound sends on transient failures\n(network errors, 429, 5xx). Set to `0` to disable retries. Default: `3`.",port:`Port to listen on for incoming webhooks.`,reply_min_interval_secs:"Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",reply_queue_depth_max:"Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",retry_base_delay_ms:"Base delay in milliseconds for exponential backoff between retries. Default: `500`.\nValues below `1` are clamped to `1ms` at runtime to avoid busy-retry loops.",retry_max_delay_ms:"Maximum delay cap in milliseconds for any single retry wait. Default: `30000` (30s).\nValues below `1` are clamped to `1ms` at runtime to avoid busy-retry loops.",secret:`Shared secret for webhook signature verification (HMAC-SHA256).
The channel will refuse to start without one. Set
\`[channels.webhook.<alias>].secret\` in config.`,send_method:"HTTP method for outbound messages (`POST` or `PUT`). Default: `POST`.",send_url:`URL to POST/PUT outbound messages to.`},WhatsAppConfig:{access_token:`Access token from Meta Business Suite (Cloud API mode)`,allowed_groups:"Allowed group chats by JID (Web mode). An empty list (the default)\npermits all groups; a non-empty list drops every group message whose\nchat JID matches no entry. Each entry matches either the full group\nJID (`123456789012345@g.us`) or the JID user part - the segment before\n`@` (`123456789012345`) - compared exactly, not as a string prefix.\nDirect messages bypass this filter regardless of list contents.\nModeled on the Matrix channel's `allowed_rooms`; it gates group\nidentity, which `dm_policy`/`group_policy` (chat type) and the\nsender allowlist (sender) do not.",app_secret:`App secret from Meta Business Suite (for webhook signature verification)
Can also be set via \`ZEROCLAW_WHATSAPP_APP_SECRET\` environment variable
Only used in Cloud API mode`,approval_timeout_secs:"Seconds to wait for operator approval on `always_ask` tools before auto-denying.",dm_mention_patterns:`Regex patterns for DM mention gating (case-insensitive).
When non-empty, only direct messages matching at least one pattern are
processed; matched fragments are stripped from the forwarded content.
Example: \`["@?ZeroClaw", "\\\\+?15555550123"]\``,dm_policy:`Policy for direct messages when mode = "personal".
"allowlist" (default) | "ignore" | "all".`,enabled:"Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",excluded_tools:`Tools excluded from this channel's tool spec. When set, these tools
are not exposed to the model when responding via this channel.`,group_mention_patterns:`Regex patterns for group-chat mention gating (case-insensitive).
When non-empty, only group messages matching at least one pattern are
processed; matched fragments are stripped from the forwarded content.
Example: \`["@?ZeroClaw", "\\\\+?15555550123"]\``,group_policy:`Policy for group chats when mode = "personal".
"allowlist" (default) | "ignore" | "all".`,interrupt_on_new_message:"Cancel an in-flight response from this channel sender when a newer\nWhatsApp message arrives. Default: `false`.",mention_only:`When true, only respond to messages that @-mention the bot in groups (Web mode only).
Direct messages are always processed.
Bot identity is resolved from the wa-rs device at runtime; \`pair_phone\` seeds it on first connect.`,mode:`Usage mode for WhatsApp Web: "business" (default) or "personal".
In personal mode the bot applies dm_policy, group_policy, and
self_chat_mode to decide which chats to respond in.`,pair_code:`Custom pair code for linking (Web mode, optional)
Leave empty to let WhatsApp generate one`,pair_phone:`Phone number for pair code linking (Web mode, optional)
Format: country code + number (e.g., "15551234567")
If not set, QR code pairing will be used`,passive_group_context:`When true in WhatsApp Web group chats, unaddressed messages that pass
sender/chat authorization are recorded as passive conversation context
without starting an agent turn. Default: \`false\`.`,phone_number_id:`Phone number ID from Meta Business API (Cloud API mode)`,proxy_url:"Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",reply_min_interval_secs:"Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",reply_queue_depth_max:"Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",self_chat_mode:`When true and mode = "personal", always respond to messages in the
user's own self-chat (Notes to Self). Defaults to false.`,session_path:`Session database path for WhatsApp Web client (Web mode)
When set, enables native WhatsApp Web mode with wa-rs`,verify_token:`Webhook verify token (you define this, Meta sends it back for verification)
Only used in Cloud API mode`,ws_url:`Override the WhatsApp Web WebSocket URL (Web mode, optional). Used
by integration tests and proxy setups; leave unset to use the
default endpoint that ships with \`wa-rs\`.`},WssConfig:{bind:`Bind address for the WSS listener (default: "0.0.0.0").`,cert_path:`Path to the PEM-encoded server certificate file.`,enabled:`Enable the WSS listener (default: false).`,key_path:`Path to the PEM-encoded server private key file.`,port:`Port for the WSS listener (default: 9781).`},XaiModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},YiModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."},ZaiModelProviderConfig:{api_key:`Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.`,chat_template_kwargs:`Arbitrary key/value pairs forwarded verbatim as \`chat_template_kwargs\`
in the request body (llama.cpp-specific). Use this to pass model-family
template variables that control behaviour not exposed by other fields.
Example (Qwen3 thinking suppression):
  \`chat_template_kwargs = { enable_thinking = false }\``,context_window:`Context window size (max input tokens) for this model.
Auto-populated on setup from provider's /models endpoint if available.
Override manually for custom endpoints or when auto-detection fails.`,extra_headers:"Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",fallback:"Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",fallback_models:"Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",kind:`Provider implementation to instantiate for this profile. Use this
when a canonical typed slot should run through a compatible
implementation, e.g. \`[providers.models.openai.proxy] kind =
"openai-compatible"\`.`,live_pricing:"Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",max_tokens:`Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.`,merge_system_into_user:`ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.`,model:"Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",native_tools:"Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",pricing:`Per-model pricing for cost tracking, USD per 1M tokens.

Free-form key/value map. Keys are user-defined model identifiers; an
optional \`.input\` / \`.output\` suffix encodes pricing dimension when
the operator wants to split rates. A bare key without a suffix is
used as a flat per-token rate when neither dimension is specified.
Default is empty: cost tracking falls back to "unknown" rates and
only token usage is recorded.

Example: \`pricing = { opus = 15.0, sonnet = 3.0 }\`
Or split: \`pricing = { "opus.input" = 15.0, "opus.output" = 75.0 }\``,provider_extra:`Extra JSON parameters to include in API requests.
Merged at the top level of the request body, allowing provider-specific
features (routing, transforms, etc.) without code changes.
Example: \`provider_extra = { model_provider = { only = ["Anthropic"] } }\``,replay_assistant_reasoning:"Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",requires_openai_auth:"When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",temperature:`Sampling temperature passed to the model. Lower values (0.0–0.3) give
deterministic, near-verbatim output, which fits code, routing, summarization.
Higher values (0.7–1.2) give more varied output, which fits open-ended chat.`,think:"Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",timeout_secs:`HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.`,tls_ca_cert_path:`Path to a PEM-encoded CA certificate for TLS connections to this provider.
Must be an absolute path; shell expansion (e.g. \`~\`) is not performed.
Leave unset to use the system's default trust store.`,uri:"Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",vision:"Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",wire_api:"Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."}};function n(e,n){return t[e]?.[n]}var r={AccessMode:[`read`,`write`,`read_write`],ApprovalDecision:[`approve`],ApprovalMode:[`both`,`out_of_band_required`,`agent_tool`],ApprovalTimeoutAction:[`escalate`,`cancel`,`auto_approve`],AuthMode:[`api_key`,`o_auth`],AutonomyLevel:[`readonly`,`supervised`,`full`],ConditionValueType:[`string`,`number`,`bool`,`enum`,`date_time`],ConfigApiCode:[`path_not_found`,`validation_failed`,`config_changed_externally`,`reload_failed`,`op_not_supported`,`secret_test_forbidden`,`value_type_mismatch`,`required_field_empty`,`invalid_numeric_range`,`invalid_format`,`invalid_enum_variant`,`dangling_reference`,`internal_error`],CronShellOutputFormat:[`wrapped`,`raw`],DelegateExecutionMode:[`bounded`,`independent`],DelegationMode:[`forbidden`,`allow`],DiscordReactionScope:[`off`,`own`,`all`],FilesystemEventKind:[`created`,`modified`,`deleted`,`renamed`],FirecrawlMode:[`scrape`,`crawl`],FlowRole:[`sequence`,`dependency`,`failure`,`switch`,`trigger`],GlmEndpoint:[`cn`,`global`],GraphSeverity:[`warning`,`error`],HardwareTransport:[`None`,`Native`,`Serial`,`Probe`],KiloEndpoint:[`gateway`],LarkReceiveMode:[`websocket`,`webhook`],LineDmPolicy:[`open`,`pairing`,`allowlist`],LineGroupPolicy:[`open`,`mention`,`disabled`],LogLlmRequestPayload:[`off`,`redacted`,`full`],LogPersistence:[`none`,`rolling`,`full`,`rotating`],LogToolIo:[`off`,`redacted`,`full`],MattermostListenMode:[`polling`,`websocket`],McpTransport:[`stdio`,`http`,`sse`],MemoryBackendKind:[`none`,`sqlite`,`postgres`,`qdrant`,`markdown`,`lucid`],MemoryDedupAction:[`reject`,`merge`],MemoryEvictOrder:[`value`,`oldest`],MinimaxEndpoint:[`cn`,`intl`],MoonshotEndpoint:[`cn`,`intl`,`code`],NodeKind:[`step`,`trigger`],NodeRunState:[`completed`,`failed`,`skipped`,`pending`,`active`],ObservabilityBackend:[`none`,`log`,`verbose`,`prometheus`,`otel`],OnNoApprover:[`deny`,`inherit-originator`],OtelContentPolicy:[`off`,`redacted`,`full`],OtpMethod:[`totp`,`pairing`,`cli-prompt`],OutputModality:[`mirror`,`voice`,`text`],PinClass:[`flow`,`data`],ProxyScope:[`environment`,`zeroclaw`,`services`],QwenEndpoint:[`cn`,`intl`,`us`,`code`],RuntimeKind:[`native`,`docker`,`cloudflare`],SearchMode:[`bm25`,`embedding`,`hybrid`],SkillsPromptInjectionMode:[`full`,`compact`],SlashCommandScope:[`global`,`guild`],SopAdmissionPolicy:[`parallel`,`hold`,`coalesce`,`drop`],SopDispatch:[`agent_loop`,`sop`,`sop_and_agent_loop`],SopExecutionMode:[`auto`,`supervised`,`step_by_step`,`priority_based`,`deterministic`],SopPriority:[`low`,`normal`,`high`,`critical`],SopRunStatus:[`pending`,`running`,`waiting_approval`,`completed`,`failed`,`cancelled`,`paused_checkpoint`],SopRunStoreBackend:[`sqlite`,`memory`],SopStepKind:[`execute`,`checkpoint`,`capability`],StepFailure:[`fail`],StepfunEndpoint:[`cn`,`intl`],StreamMode:[`off`,`partial`,`multi_message`],ThinkingLevel:[`off`,`minimal`,`low`,`medium`,`high`,`max`],TodoTrackerLocation:[`bottom`,`left`,`right`],ToolFilterGroupMode:[`always`,`dynamic`],TriggerFieldKind:[`text`,`list`,`expression`],UpgradeStatusState:[`idle`,`running`,`done`,`restarting`,`failed`],VoiceProvider:[`twilio`,`telnyx`,`plivo`],WhatsAppChatPolicy:[`allowlist`,`ignore`,`all`],WhatsAppWebMode:[`business`,`personal`],WireApi:[`responses`,`chat_completions`],ZaiEndpoint:[`cn`,`global`]};function i(e){return r[e]??[]}function a(e,t){return n(e,t)}var o=i(`SopPriority`),s=i(`SopExecutionMode`),c=i(`SopStepKind`),l={node_w:210,node_h:84,col_gap:130,row_gap:46,origin:24};function u(e){return e.layout.geometry??l}async function d(){return(await e(`/api/sops`)).sops??[]}async function f(t){return(await e(`/api/sops/runs${t?`?sop=${encodeURIComponent(t)}`:``}`)).runs??[]}function p(t){return e(`/api/sops/${encodeURIComponent(t)}/graph`)}var m=null;function h(){return m||=e(`/api/sops/graph-legend`),m}function g(e){let t=new Map;for(let n of e??[])t.set(n.key,n.description);return t}function _(e){let t=new Map;for(let n of e??[])t.set(n.key,n.label);return t}function v(t){return e(`/api/sops/${encodeURIComponent(t)}/full`)}function y(t){return e(`/api/sops`,{method:`POST`,body:JSON.stringify(t)})}function b(t){return e(`/api/sops/${encodeURIComponent(t.name)}`,{method:`PUT`,body:JSON.stringify(t)})}function x(t){return e(`/api/sops/${encodeURIComponent(t)}`,{method:`DELETE`})}function S(t,n){return e(`/api/sops/wire-draft`,{method:`POST`,body:JSON.stringify({sop:t,edit:n})})}function C(t){return e(`/api/sops/graph-draft`,{method:`POST`,body:JSON.stringify({sop:t})})}function w(){return e(`/api/sops/trigger-sources`)}function T(e,t){let n=e??``,r=n.trim(),i=[...t].map(e=>e.token).sort((e,t)=>t.length-e.length),a=r.startsWith(`$`),o=a?r.replace(/^\$\.?/,``):r;for(let e of i){let t=o.indexOf(e);if(t<0)continue;let r=o.slice(0,t).trim(),i=o.slice(t+e.length).trim();return{path:a?r:null,op:e,value:i,raw:n}}return{path:a?o.trim():null,op:``,value:``,raw:n}}function E(e){if(e.op.length===0)return null;let t=`${e.op} ${e.value}`.trim();if(e.path===null)return t;let n=e.path.trim();return n.length>0?`$.${n} ${t}`.trim():t}function D(t,n){return e(`/api/sops/${encodeURIComponent(t)}/runs/${encodeURIComponent(n)}/overlay`)}function O(t,n){return e(`/api/sops/${encodeURIComponent(t)}/run`,{method:`POST`,body:JSON.stringify({payload:n??null})})}function k(t,n,r){return e(`/api/sops/${encodeURIComponent(t)}/runs/${encodeURIComponent(n)}/decide`,{method:`POST`,body:JSON.stringify(r)})}function A(e){let t=new Map;for(let n of e?.nodes??[])t.set(n.step,n.state);return t}function j(e){let t=new Map;for(let n of e?.nodes??[])n.tool_calls&&n.tool_calls.length>0&&t.set(n.step,n.tool_calls);return t}var M={accent:`neutral`,success:`ok`,error:`error`,warning:`warn`,neutral:`neutral`};function N(e){return M[F(e)]}function P(e){return M[I(e)]}function F(e){switch(e){case`active`:return`accent`;case`completed`:return`success`;case`failed`:return`error`;case`skipped`:return`warning`;default:return`neutral`}}function I(e){switch(e){case`running`:case`pending`:return`accent`;case`completed`:return`success`;case`failed`:return`error`;case`waiting_approval`:case`paused_checkpoint`:return`warning`;default:return`neutral`}}function L(e){switch(e){case`completed`:case`failed`:case`cancelled`:return!0;default:return!1}}function R(e){switch(e){case`failure`:return`error`;case`dependency`:return`warning`;case`switch`:return`switch`;case`trigger`:return`accent`;default:return`success`}}export{w as A,F as C,a as D,s as E,o as O,N as S,b as T,d as _,x as a,T as b,D as c,C as d,g as f,f as g,u as h,k as i,S as j,c as k,v as l,L as m,E as n,R as o,_ as p,y as r,h as s,l as t,p as u,j as v,P as w,O as x,A as y};